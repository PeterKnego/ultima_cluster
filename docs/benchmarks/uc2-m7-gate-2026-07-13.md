# uc2 M7 gate — live single-server reconfiguration

**Date:** 2026-07-13 (fleet PASS: 2026-07-14)

> **✅ M7 GATE PASSED — 2026-07-14 (fleet).** This doc ships the M7 correctness
> proofs (sim invariants + counterfactual-red pins, the L3 reconfig-churn
> lincheck capstone, the SIGKILL-mid-config-window crashtest, the full
> regression suite) AND the fleet verdict: on 5×`c6id.2xlarge` under
> unthrottled load with snapshots+purge paired, **every reconfig transition's
> commit-rate dip landed at 0.0–4.7 %** (gate < 10 %) and **leader
> self-removal handed off in 3.22 s** (gate < 10 s) with zero committed loss
> and zero read divergence. The first fleet run was an honest FAIL — all of it
> gate-harness measurement, dissected in "What the fleet taught" below.
> `v2.1.0` is tagged on this PASS.

## What the gate measures

Spec §9.6 (`docs/superpowers/specs/2026-07-13-uc2-reconfig-design.md`):

| # | deliverable | gate |
|---|---|---|
| M7 | live single-server reconfiguration | replace-a-box + resize 3↔5 + leader self-removal, all under sustained load |

M7 is the milestone that makes cluster membership a **live, online operation**
instead of boot-time-only configuration. It closes v2.0's one accepted
capability regression against the reference design's original ambition
("static voting set … joint-consensus reconfiguration is v2.x" — the v2.0
README/CLAUDE.md scope line this milestone rewrites). Unlike v1 (which never
shipped dynamic membership) and unlike **OSS Aeron Cluster**, which shipped
"dynamic join" (passive members + log-recorded membership events —
structurally the same idea) and then **deprecated it in 1.41 and removed it in
1.42 (2023)** as never production-quality — current Aeron is static-membership
with offline procedures, and its vendor's production answer for replacement
moves the operation *out* of the consensus protocol entirely. M7 deliberately
exceeds the reference here: the bet is that the membership × snapshot ×
truncation × recovery integration surface that sank Aeron's version is exactly
what `uc2_sim` + the WGL capstones + the crashtest were built to hold.

The three fleet scenarios, and how each is proven locally first:

1. **replace-a-box** — add a learner on a 5th host, catch it up, promote it to
   voter, remove a (crashed) original voter. Fleet gate: commit-rate dip
   **< 10 %** across every transition window, zero read divergence.
2. **resize-3-5-3** — two add+promote pairs (3→5 voters), then two
   demote+remove-learner pairs (5→3), landing back on the original ids. Same
   bars as replace-a-box.
3. **leader-self-removal** — the serving leader removes itself. Fleet gate:
   zero committed loss; new leader serving **< 10 s** (the measured gap is
   reported regardless — it is the existing ~200 ms failover class, not a new
   number to hit).

## Correctness proofs (local, green)

### Sim: inv6–9 + counterfactual-red pins (the mechanism proof)

`uc2_sim` models config frames as data (adoption-on-durable, rebuild-at-boundary,
revert-on-truncate) with four new invariants alongside inv1–5:

- **inv6** config determinism — adopted config equals the durable-frontier-implied config.
- **inv7** quorum legality — commit only advances via a quorum of the adopting config, config-aware (generalizes the old phantom-commit oracle).
- **inv8** revert correctness — after any truncation, adopted config re-equals the frontier's config.
- **inv9** tombstone permanence — a tombstoned id never re-enters.

Counterfactual-red pins (the C-1 discipline — delete a guard, assert the
invariant goes red):

- `counterfactual_no_serving_gate_produces_disjoint_quorum_commit` — Ongaro's
  2015 correction staged mechanically (an isolated leader proposes a promote,
  crashes before the frame commits; with the serving gate disabled, a second
  leader commits a REMOVE on a disjoint 2-of-3 quorum) — asserts the inv7
  violation. `serving_gate_refuses_the_premature_proposal` is the guarded twin
  (same instant, gate ON → `Err(NotServing)`, stays green).
- `counterfactual_no_revert_breaks_inv8` — deleting revert-on-truncate on the
  identical divergent-leader trace asserts the inv8 violation.

Plus `add_promote_demote_remove_cycle_under_faults` (crash + partition between
steps, both removed nodes `halted_removed`, survivors converge and keep
committing, re-adding a tombstoned id refused) and
`propose_during_pending_is_refused`. **Fuzz arm:**
`fuzz_heavy_config_churn` (sim-heavy) — 1000 seeds, random legal + illegal
config ops (one guaranteed-illegal op per round asserted `Err`) from random
nodes under drop/dup/crash churn; refused-never-adopted pinned
(`node_config_version(i) <= accepted` for every node); all invariants green.
The fuzz arm **genuinely bit** during development: seed 0 went red on first
run, exposing a real oracle-vs-mechanism mismatch in the initial inv7 (the
mid-self-removal leader's "+1 own slot" tracker rule was stricter-checked than
the mechanism warrants) — fixed in the oracle, not the mechanism. That is the
milestone thesis working mechanically, not anecdotally.

```
cargo test -p uc2_sim                                → 20 + 18 + 0 doc, all green
cargo test -p uc2_sim --features sim-heavy --release → 20 + 20, all green (incl. fuzz_heavy_config_churn, 1000 seeds)
cargo test -p uc2_consensus                          → 67 green (incl. 6 M7-revert tests)
```

### L3: WGL reconfig-churn capstone (the concurrency proof)

`uc2_node/tests/lin_v2.rs::linearizable_under_reconfig_churn` — the same
seeded workers + WGL checker as the failover/purge capstones, with a 4th fault
arm (1-in-4): a spare node cycling `add-learner → promote → demote →
remove-learner` mid-churn, alongside kill-leader / follower-service-crash /
partition-minority+heal. Non-vacuity is asserted (`config_ops_committed ≥ 3`),
not assumed — a run where the config arm barely fires would silently prove
nothing about reconfiguration.

Per-seed results (from the reconfig-churn L3 authoring; each seed run at
least twice, independently):

| seed | run | faults | config-arm picks | config_ops_committed | ops | ok-rate | elapsed | verdict |
|---|---|---|---|---|---|---|---|---|
| 0x1107 | 1 | 22 | 8 | 8 | 621 | 99.2% | 59.2 s | Linearizable |
| 0x1107 | 2 | 34 | 12 | 11 | 615 | 98.0% | 97.7 s | Linearizable |
| 0x1107 | 3 | 16 | 4 | 4 | 645 | 99.5% | 44.0 s | Linearizable |
| 7 | 1 | 14 | 4 | 4 | 615 | 99.7% | 40.9 s | Linearizable |
| 7 | 2 | 40 | 13 | 9 | 612 | 99.3% | 60.8 s | Linearizable |
| 99 | post-fix, run 2 | 44 | 5 | 4 | 616 | 98.7% | 74.8 s | Linearizable |
| 99 | post-fix, run 3 | 49 | 7 | 5 | 614 | 98.0% | 82.0 s | Linearizable |

Every PASS run: ok-rate ≥ 98% (well above the 80% liveness bar),
`config_ops_committed ≥ 4` (above the ≥ 3 non-vacuity bar), elapsed
comfortably under the 120 s/seed budget. (Seed 99's first attempt, pre-fix,
drew the config arm only twice in the whole budget and failed the non-vacuity
bar — root-caused to per-seed arm-pick variance, not a stall; fixed by
shortening `FAULT_PERIOD` 2.5 s → 1.2 s, confirmed clean on 2 further runs.)

**The reconfig-churn capstone's budget is env-tunable**:
`UC2_LIN_BUDGET_SECS` (default `120`; the scheduler's soft cutoff is
`budget − 5`). Nightly CI sets `UC2_LIN_BUDGET_SECS=240` at the job level for
extra headroom on a busier/smaller CI box; the other three capstones keep
their original hard-coded budgets untouched.

```
cargo test -p uc2_node --test lin_v2 -- --test-threads=1   → 4/4 (failover, purge/snapshot churn, reconfig-churn, smoke)
```

### Crashtest: SIGKILL mid-config-window

`examples/uc2-crashtest/tests/hard_crash.rs::sigkill_mid_config_window` — a
REAL 3-process multi-node cluster (the crate's first multi-node scenario),
racing a real SIGKILL of the leader's node PROCESS against its own
`AddLearner` admin write, restarting it, and proving cluster-wide
config-version convergence never straddles two values (`observed_pending`
catches the append-to-commit race window on some runs; both "committed" and
"reverted" outcomes are valid and both are observed).

Race-side statistics across 5 independent invocations (3 races each, 15 total
race attempts):

| invocation | outcomes (pending?, final version) | ok-rate | verdict |
|---|---|---|---|
| A | committed, committed, committed (1 pending=T) | 350/353 | Linearizable |
| B | reverted, committed, committed (1 pending=T) | 336/339 | Linearizable |
| C | reverted, reverted, reverted | 378/379 | Linearizable |
| D | committed, reverted, committed (1 pending=T) | 377/379 | Linearizable |
| E (final gate) | reverted, committed, committed (1 pending=T) | 316/319 | Linearizable |

Totals: **9/15 committed, 6/15 reverted** (both sides of the race observed
repeatedly, as required), `observed_pending=true` on 4/15 (the append-to-commit
window genuinely caught). Every run: config-version convergence landed on
exactly one value cluster-wide (never a mix), full concurrent WGL history
stayed `Linearizable`.

```
cargo test -p uc2-crashtest --features hard-crash-tests   → 4/4 (smoke, service-sigkill, node-sigkill-recovery, config-window)
```

### Regression suite (green)

```
cargo test -p uc2_node --test reconfig -- --test-threads=1
```
10/10: `add_learner_via_leader_cnc_is_accepted_and_converges`,
`add_learner_via_follower_cnc_is_forwarded_and_converges`,
`leader_self_removal_hands_off`, `removed_follower_halts_and_zombie_cannot_disrupt`,
`joining_node_boots_from_stale_seed`, `full_replace_a_box_recipe`,
`resize_3_to_5_to_3`, `every_refusal_surfaces`, `truncation_revert_e2e`,
`crash_mid_pending_recovers`.

## Review-caught bugs

Evidence the verification stack does its job — three real bugs found and
fixed during M7's authoring, not by inspection but by tests/gate work actually
exercising the mechanism hard enough:

1. **`uc_protocol` MPSC free-space underflow under producer contention**
   (`uc_protocol/src/ring/mpsc.rs`) — a stale `claim_pos` snapshot vs. a
   freshly-reloaded `consumer_pos` could underflow-panic in debug builds under
   sustained multi-producer contention (SPSC has no equivalent window; it has
   exactly one producer). Pre-existing in v2.0, never exercised hard enough to
   surface until the L3 reconfig-churn capstone's spare-node traffic did.
   Fixed with `saturating_sub` (matches the existing outer-call style);
   pinned with a deterministic regression test that forges the exact
   `claim_pos < consumer_pos` shape directly rather than racing threads.
   **Flag for the v2.0.x release notes** — this is a pre-existing bug in
   already-shipped v2.0, found by M7's proof stack, not introduced by M7.
2. **Absence-based halt predicate conflated "not yet admitted" with
   "removed"** (`uc2_consensus::election::ElectionSm::adopt_config`) — the
   original `self_removed` latch fired whenever the JUST-adopted config
   excluded the node's own id, with no way to distinguish a real removal from
   replaying a HISTORICAL config (from before the node joined) during
   catch-up, where a LATER config in the same catch-up run legitimately
   re-includes it. Caught by `resize_3_to_5_to_3` (a second learner
   sequentially and correctly absent from v1/v2, present at v3 — the old
   predicate would have permanently halted it on v1). Fixed by switching to a
   **tombstone-based** predicate (`config.tombstones.contains(&self.id)`) —
   ids are fresh-forever, so absent-and-not-tombstoned unambiguously means
   "not yet admitted."
3. **`publish_peer_band` never cleared trailing cnc peer slots on a SHRINKING
   rebuild** (`uc2_node/src/node.rs`) — found by this Task 11's own gate
   authoring, not by an earlier task. The republish loop only ever wrote
   `0..peer_band.len()`; a rebuild that reduced the peer count (a
   demote/remove-voter/remove-learner) left a stale, uncleared `id_and_role`
   slot at the OLD (now out-of-range) index — surfacing as a ghost duplicate
   entry for a still-live id in `uc2ctl status`'s member listing (and in this
   gate's own peer-band-based catch-up checks). **Diagnostics-only band —
   never gated correctness** (the real membership/quorum state lives in the
   SM's own rebuilt tracker), but a real, user-visible bug in operator-facing
   tooling. Fixed by zeroing every slot from `peer_band.len()..CNC_MAX_PEER_SLOTS`
   whenever the band republishes. Confirmed via `uc2_node/tests/reconfig.rs`
   (unaffected, 10/10 still green — that suite never asserted "no duplicate
   ids", only "the removed id never surfaces again", which is why this
   shipped unnoticed until a stricter check.

## Known issues / follow-ups (NOT M7 scope)

- **CLOSED 2026-08-03, to a pre-committed standard: 0 hits in 151 runs, per-run
  rate under 1.96 % at 95 % confidence.** The sample size was fixed at 150
  BEFORE the fix was written — chosen to exclude both the 11.3 % receive-path
  population and the ~1.6 % `end=0` population — precisely because the two
  earlier "fixed" calls came from 20-run streaks and both were wrong. For scale,
  the pre-fix rate would have produced ~30 hits over 151 runs.

  The last defect was the **`end=0` population (7 of the 50 hits): the archive's
  own cursor found mid-frame.** `durable` sat exactly 32 B inside a 64 B frame
  with `sent` marking that frame's true start — the archive had recorded a 32 B
  NewTerm there and a 64 B data frame replaced it AFTERWARDS, i.e. a rewrite
  BELOW `durable`, over bytes already in the journal. Cause: a node that has
  been LEADER pushes `append`/`durable` far past its own receive frontier (its
  appender writes, its archive records, its receiver accepts nothing meanwhile);
  on step-down that stale-low frontier accepts the next leader's DATA at
  recorded positions. There was a resync for the counter moving DOWN (a prime)
  and one for a snapshot floor moving UP, but none for this. Fixed by (a)
  rebasing the receive frontier UP to the shared counter — it is authoritative
  for what this node holds — and (b) `PositionedWriter::write_run` refusing
  `position < durable`, enforcing the `[append, durable+capacity)` writer-owned
  contract its own SAFETY comment had always claimed but only half-enforced.
  Red-verified independently: with only (b) the recorded bytes survive but the
  follower WEDGES; with only (a) it converges and nothing is overwritten.

  **A DIFFERENT and more serious finding is now open**, caught by the same soak
  (run 109, unmutated): `CommittedTruncationWitness` fired — the committed
  frontier held by NO node, 114,080 B short, all three nodes below it. That is
  acked-write loss rather than a fail-stop, at ~0.7 %/run (1 in 151). Not yet
  established whether it is a real leader-completeness violation or a false
  positive of the witness (its soundness rests on no node's `commit` ever
  running ahead of what was genuinely committed — the Finding #6b class). Log:
  `/home/claude/elle-out/soak2/hits/other-20260803T070457-run109.log`.

  ---

  The history below is kept as written, including its wrong verdicts.

- **The `RecorderCorrupt` archive fail-stop RECURRED on unmutated `main`,
  2026-08-02, and the first fix (b9ea713) DID NOT CLOSE IT.**
  A 444-run soak on b9ea713 itself measured **50 hits — an 11.3 % per-run
  failure rate (95 % CI 8.4–14.3 %)**. The entry below described this as fixed
  on the strength of a 20-run clean streak; that sample was underpowered and the
  claim was wrong. Read the whole entry with that correction in mind.

  What survives the correction: the three guards ARE real defects, each with a
  red-verified regression test, and they stay. What does not: any claim that
  they are the whole bug. Nor can an improvement be asserted — the pre-fix
  baseline was 2 hits in 8 runs, whose 95 % interval spans 3–65 % and therefore
  contains 11.3 %.

  **The 50 hits split into two populations**, which is new information the small
  samples could never have shown:
  - **43 hits — the receive-path over-claim** described below: `append`
    published past the last real frame, by 32 B to 907 KB, walk fails partway in.
  - **7 hits with `end=0`** — the walk fails at `from` ITSELF, i.e. the
    ARCHIVE'S OWN CURSOR sits mid-frame. That is a different failure, closer in
    shape to issue #6's plane than to the receiver's, and several of those also
    show `sent` inconsistent with `append` in ways the receive-path story does
    not explain. Six-plus clean examples of a sharp signature is the tractable
    end; start there.

  Logs: `/home/claude/elle-out/soak/hits/` on the dev box, each self-diagnosing
  (frame walk with per-frame terms + forward scan past the failure). **Do not
  re-run under hot-path tracing — it suppresses the race** (0 hits/36 traced
  runs vs 3/14 untraced); that is why the post-mortem does the forensics itself.

  **METHOD NOTE, the expensive lesson here:** every intermediate verdict in this
  investigation was drawn from ≤20-run samples of a ~10–25 % event, where a
  clean streak is unremarkable — `0/20` at an 11 % rate happens 9 % of the time.
  Two separate "fixed" calls were made and both were wrong. For a probabilistic
  failure, the sample size has to be set from the rate you are trying to
  exclude, BEFORE reading the result as a verdict.

  ---

  The original (partially superseded) entry follows.

- **The `RecorderCorrupt` archive fail-stop RECURRED on unmutated `main`,
  2026-08-02 — a SECOND, unrelated cause, in the receive path.** Issue #6 (below)
  is real and its fix stands; it was simply not the only producer of this
  signature. Found by an append-heavy partition-churn workload (~2 hits per 8
  runs), and fixed by three guards in `uc2_net`'s `FollowerReceiver`, each with
  a red-verified regression test:
  1. **Cross-term out-of-order runs.** A term boundary re-frames the stream
     above the commit point — the same position carries a 96 B data frame in
     term T and the next term's 32 B NewTerm in T+1. Both sit ABOVE the
     contiguous frontier, so the accept rule (`position < contiguous`) rejects
     neither, and `Rebuilt` UNIONS their spans (it tracks positions only, by
     design). `append` is then published over a span the buffer does not tile.
     Fix: discard out-of-order runs when the term moves.
  2. **A prime landing between two datagrams of one drain.**
     `resync_after_truncation` ran once per duty cycle; a cycle drains up to 64
     datagrams and the archive primes on its own thread. Fix: resync per
     datagram.
  3. **The window between that resync and the `gen0` sample** — a few
     instructions, which `prime_generation` cannot see because `gen0` already
     carries the post-prime value. Fix: the receiver records the frontier it
     last published and refuses to publish when the live counter is found below
     it. Only a prime moves the counter backward, so this observes the prime's
     RESULT rather than racing it.

  Evidence, all on builds whose hot path carries no instrumentation (per-event
  tracing suppressed the race — 0 hits in 36 traced runs against 3 in 14
  untraced, which is why the post-mortem now does the forensics itself, walking
  frames and reporting each one's term at zero runtime cost):

  | build | hits |
  | --- | --- |
  | baseline | 2 / 8 |
  | fixes 1+2 | 3 / 14 |
  | fixes 1+2, forensic build | 1 / 10 |
  | fixes 1+2+3, forensic build | 0 / 20 |
  | fixes 1+2+3, 444-run soak | 50 / 444 — NOT FIXED |
  | **+ upward resync & write_run lower bound, 151-run soak** | **0 / 151 — CLOSED** |

  The decisive forensic dump: 15 frames of term 45 tiling 928 B from `from`,
  then term **1** frames — ring content from a previous lap — while `append`
  claimed 101,024 B. The appender cannot produce that, so the receiver published
  it. Explainer: `docs/notes/uc2-two-framings-one-position.md`. **The fail-stop
  was the lucky outcome**: had the orphaned payload's first word passed as a
  plausible length, the archive would have recorded previous-lap bytes into the
  journal as current-term data and served them on deep-NAK replay.

- **`uc2_log::archive.rs`'s `Replay::next` — a one-off `range end index 36 out
  of range for slice of length 32` panic** (observed once during M7 Task 10's
  L3 authoring in the PRE-EXISTING `linearizable_under_failover_v2` capstone;
  no reconfig code path involved). Status: **ROOT-CAUSED AND FIXED 2026-07-30**
  — see the resolution note at the end of this entry (issue #6). The hardening
  below is what caught it: the fail-stop fired in the nightly `elle` job with a
  diagnosable payload.
  - **Fail-stop conversion (Tasks 1-2):** the two OOB sites are now checked
    errors, not slice panics — `Replay::next` returns `ArchiveError::CorruptBlock`
    on a sub-header remainder or an over-running frame length; the `do_work`
    frame walk returns `ArchiveError::RecorderCorrupt` on an impossible length
    word in the committed region. A malformed block now fails the archive loudly
    instead of silently recording garbage.
  - **Repro attempt (Task 3):** a dedicated concurrent stress harness
    (`uc2_log/tests/archive_stress.rs`: lock-free appender + archiver `do_work`
    + a LOCK-FREE `Replay` drain over a `journal_arc()` clone — journal reads
    genuinely racing journal appends + fsyncs, the original panic's described
    "scan against a block still being concurrently written" — wrap-heavy on a
    64 KiB buffer, with truncation and drop+reopen arms) did **not** reproduce
    either structured error across 30 × 60 s arm-executions (two waves of 5
    iterations × 3 arms — the first with the replay serialized behind the
    archive mutex, the second fully lock-free — ~1800 stress-seconds; arm A
    recorded ~0.04-0.7 GB per run, and the lock-free wave's ~10× lower append
    throughput vs. the serialized wave confirms reads really contend appends
    inside the journal) plus every CI-budget run. The harness fails the test if
    a `CorruptBlock`/`RecorderCorrupt` ever surfaces, so it is a live
    regression guard, not just a one-shot.
  - **Hypothesis audit (Task 3):** H1 (torn `recordable_slice` walk) — the walk
    is sound given `[from, append)` immutability + a frame-boundary `from`, BUT
    the audit found a residual gap: a torn length word in `[1, HEADER_LEN)`
    aligned up to 32 and slipped past both consistency disjuncts as a "valid"
    sub-header frame. **Fixed** by widening the guard to `len < HEADER_LEN`
    (mirroring `Replay::next`/`observe_terms`), with a red-verified regression
    test. H2 (truncation seam) — sound: the archive agent serializes
    `truncate_to`+`prime` against its own `do_work` on one thread, so
    `recordable_slice(from)` never walks stale pre-truncation offsets; the
    cross-thread receiver/prime race is separately guarded by `prime_generation`
    (M6 Task 9). H3 (journal pending-tail read) — sound: `Journal::read` serves
    the append-readable tail from an exact in-memory copy and CRC-verifies
    segment-backed records, so a torn/short payload cannot reach `Replay` — and
    exercised, not just audited: the harness's lock-free replay arm drains
    `Replay::next` concurrently with `do_work`'s appends/fsyncs. H4
    (restart counter priming) — sound: `prime(pos)` sets `append == durable ==
    pos`, so `recordable_slice` returns empty until the appender re-writes fresh
    frames from `pos`; no stale-offset walk. The original one-off remains
    **unexplained but no longer reachable as a panic** (fail-stop) and
    unreproduced; the leading residual suspect (the H1 sub-header word) is now
    closed.
  - **RESOLUTION (2026-07-30, issue #6).** The fail-stop fired in the nightly
    `elle` job ([run 30518147100](https://github.com/PeterKnego/ultima_cluster/actions/runs/30518147100),
    `elle_partition`): `RecorderCorrupt { from: 143520, append: 143552, end: 0,
    claimed_len: 3120235264 }`. Root cause: **`Action::BecomeLeader` was a
    second, cross-thread backward primer.** It ran `counters().prime(base)` on
    the CONSENSUS agent, where `base = ElectionSm::durable` — a value refreshed
    in `Consensus::do_work` step 2 but consumed by the vote drain in step 1, so
    up to a full duty cycle stale. The ARCHIVE agent, on its own thread, could
    have fsynced another block in between, leaving its private `durable_pos`
    strictly ABOVE `base`; nothing reset it, because unlike `ArchiveCmd::Truncate`
    this prime never routed through the archive. The new leader then rewrote the
    buffer from `base` with a different frame layout, and the archive's cursor
    was left mid-frame — reading a payload byte as a length word.
    - **H2 was wrong, and this is why.** It ruled the seam sound because "the
      archive agent serializes `truncate_to`+`prime` against its own `do_work` on
      one thread". True of `ArchiveCmd::Truncate`, but `BecomeLeader` is a
      *second* primer the audit did not enumerate. Same reason Task 3's
      `archive_stress.rs` never reproduced it: appender/truncate/reopen arms, no
      leader-open collapse from another thread.
    - **Second consequence, worse than the panic:** because leader open never
      routed through the archive, the JOURNAL was never cut at `base`. The span
      `[base, archive.durable_pos)` was rewritten in the buffer by the new leader
      but never re-recorded, so the journal retained the OLD bytes for a range
      the buffer now held NEW bytes for — and deep-NAK replay serves from the
      journal.
    - **Fix:** `ArchiveCmd::Collapse { epoch, to }` — leader open commands the
      cut to the archive agent and completes (fresh `Appender`, NewTerm frame,
      gate reopen) on the ack, in `Consensus::on_collapsed`. The same discipline
      `Truncate` always had: the cut runs ON the archive thread, serialized
      against its own `do_work`, resetting `durable_pos` and cutting the journal,
      and bumping `prime_generation` for the receiver. Its own ack slot, since a
      reconcile truncation can still be in flight when an election is won. In the
      common (non-racy) open `to == durable_pos` and `truncate_to` is a cheap
      `Ok(())` no-op.
    - **What splitting the open across two cycles cost, and how it is paid.**
      Making leader open two-phase opened a window that the synchronous prime did
      not have: between phase 1 and the archive's ack, `do_work` step 2 would
      read the still-uncollapsed frontier and feed `DurableAdvanced`. Because
      `ElectionSm::durable` is a monotonic max and the collapse's whole premise is
      that this frontier sits ABOVE `base`, that would latch the SM above bytes
      about to be cut away, with no path back down (the `min` clamp belongs to
      the reconcile path's `Event::Truncated`, which a collapse does not
      produce) — an inflated `last_durable` vote credential and, worse, a commit
      tracker advanced with an `own_durable` the node does not physically hold.
      Step 2 is therefore suppressed while `pending_leader_open` is set; `base`
      IS `sm.durable`, so suppressing it leaves the SM exactly where it already
      was. Red-verified by
      `a_pending_leader_open_suppresses_durable_feeds_from_the_stale_frontier`.
    - **Two deliberate pieces of dead defence.** The `Collapse` arm clamps its
      cut to the archive's frontier, and `on_truncated`'s gate reopen carries a
      `pending_leader_open.is_none()` predicate. Both matter only if a reconcile
      truncation and a leader open are in flight at once — which cannot happen: a
      reconcile-truncating node cannot win an election (candidacy needs
      `Event::Tick`, dropped by the SM's truncating latch; and reaching
      `reconcile_term_map` means adopting a map, which for a higher term runs
      `BecomeFollower` first, while a same-term map at a candidate would mean two
      leaders in one term). Kept because unclamped that interleaving would
      `PositionPurged` → kill the archive agent → leave a silent non-serving
      leader-elect, which is worse than what it replaced.
      `a_reconcile_truncating_node_cannot_also_open_a_leader_term` fails loudly
      if the unreachability ever stops holding.
    - **Tests:** `uc2_log`'s
      `become_leader_collapse_below_archive_cursor_corrupts_the_walk`
      (deterministic reproduction of the exact CI signature) and
      `collapse_routed_through_the_archive_keeps_the_walk_intact`; `uc2_node`'s
      `leader_open_routes_the_collapse_through_the_archive`,
      `a_collapse_ack_after_stepping_down_does_not_resurrect_leadership`,
      `a_pending_leader_open_suppresses_durable_feeds_from_the_stale_frontier`,
      and `a_reconcile_truncating_node_cannot_also_open_a_leader_term`. Note the
      first `uc2_log` test asserts the BUG (a characterization test — the fix
      lives in `uc2_node`); its doc comment says so, since a future change to
      `recordable_slice` could turn it red for the right reasons.
    - `LogBuffer::corrupt_report` now turns any future `RecordableCorrupt` into a
      full post-mortem (counters, the straddling frame if any, a hexdump around
      the cursor) rather than four numbers — the information gap that made the
      original sighting un-diagnosable.

## `m7_gate` sandbox smoke (NOT the gate)

`cargo run -p uc2_node --release --example m7_gate -- all --secs 6`. Three
voters + services + a background write-load driver, all in-process (real
file-backed shmem), running all three M7 scenarios sequentially. In-process
PASS criteria (per the task brief, looser than the fleet bars): every
transition's admin op is accepted and converges, linearizable reads stay
monotonic across the whole run, and commit keeps flowing throughout every
transition — the strict **< 10 % dip / < 10 s self-removal-gap bars are
fleet-only**, printed here as informational (identical discipline to M6's
smoke). Verbatim console (one dev box, core-starved across 3 nodes' agents +
services + the load driver + admin cnc round-trips):

```
== uc2 M7 gate: SMOKE (local, in-process; 3 voters + services + load) ==
*** THIS IS NOT THE M7 GATE *** — one dev box, core-starved; the real bars (<10%
commit-rate dip across every reconfig transition, <10s self-removal failover) are only
claimable on the 5-host c6id fleet run. See docs/benchmarks/uc2-m7-gate-2026-07-13.md.
leader elected: n1
node 60: removed from cluster config — halting
node 61: removed from cluster config — halting
node 1: removed from cluster (self-removal committed) — halting

== M7 gate smoke results ==
  [PASS] replace-a-box — add-learner 50 -> catch-up -> promote (v2) -> remove 0 (v3) in 0.02s; commit advanced throughout (91866/s — informational, the <10% dip bar is fleet-only)
  [PASS] monotonic-read — read 5619 after replace-a-box (>= previous 0)
  [PASS] resize-3-5-3 — 3->5 (add+promote 60, add+promote 61) -> 5->3 (demote+remove 60, demote+remove 61) in 0.03s; final voter set == original 3; commit advanced throughout (127693/s — informational, the <10% dip bar is fleet-only)
  [PASS] monotonic-read — read 5675 after resize-3-5-3 (>= previous 5619)
  [PASS] leader-self-removal — gap 0.178s (fleet bar <10s; in-process budget 30s to absorb core-starvation noise), zero committed-high-water loss, new leader n2 serving and committing
  [PASS] monotonic-read — read 6753 after leader-self-removal (>= previous 5675)
RESULT: PASS (smoke) — all three M7 scenarios held locally.
```

Stable across 4 repeated runs (dip numbers vary run to run as expected on a
shared box; the PASS criteria that actually gate here — acceptance,
convergence, monotonic reads, non-zero commit rate through every
transition — held every time). This smoke is where the `publish_peer_band`
bug above was actually caught: `resize-3-5-3`'s final-voter-set check failed
with a duplicate id in the peer band until the fix landed.

## Multi-process validation (real separate processes, real UDP — local)

`bench-infra/scripts/m6_fleet_gate.py --m7 --local` — **this is the task's
headline gate.** Five real separate OS processes (3 voters + 2 spare-host
slots reused across scenarios, loopback UDP), admin ops driven through the
separate `uc2ctl` binary exactly as a fleet run would (subprocess locally,
ssh on the real fleet), observing every node only through `m7_gate probe`.
This is a materially stronger proof than the in-process smoke: it exercises
the REAL `uc2ctl` binary, REAL cross-process forwarding/adoption, and the
SAME dip/gap measurement technique the fleet run uses (fixed baseline +
measurement windows spanning each transition — the identical trick M6's
`learner-join` used).

Representative run (one dev box, 4 cores, core-starved across 5 node
processes + 5 service processes + admin/probe subprocess spawns) — real
console output, verbatim:

```
[m6-gate] == M7 LOCAL (5 procs, loopback UDP) gate ==
[m6-gate] leader elected: node1
[m6-gate] [PASS] replace-a-box — add-learner 10 -> catch-up -> promote -> remove 0 (new voter set host-ids [1, 2, 10]); per-transition dip: add+catchup=0.0%, promote=2.2%, remove=0.0% (gate <10%)
[m6-gate] [PASS] resize-3-5-3 — 3->5 (add+promote 11, add+promote 12) -> 5->3 (demote+remove 11, demote+remove 12); final voter set == the set this scenario started with; per-transition dip: add-11=0.0%, promote-11=1.3%, add-12=0.0%, promote-12=0.0%, demote-11=0.0%, remove-11=0.0%, demote-12=0.4%, remove-12=3.4% (gate <10%)
[m6-gate] [PASS] leader-self-removal — gap=0.43s (gate <10s), zero committed-high-water loss, new leader node2 serving_and_committing=True
[m6-gate] == results ==
[m6-gate]   [PASS] replace-a-box — add-learner 10 -> catch-up -> promote -> remove 0 (new voter set host-ids [1, 2, 10]); per-transition dip: add+catchup=0.0%, promote=2.2%, remove=0.0% (gate <10%)
[m6-gate]   [PASS] resize-3-5-3 — 3->5 (add+promote 11, add+promote 12) -> 5->3 (demote+remove 11, demote+remove 12); final voter set == the set this scenario started with; per-transition dip: add-11=0.0%, promote-11=1.3%, add-12=0.0%, promote-12=0.0%, demote-11=0.0%, remove-11=0.0%, demote-12=0.4%, remove-12=3.4% (gate <10%)
[m6-gate]   [PASS] leader-self-removal — gap=0.43s (gate <10s), zero committed-high-water loss, new leader node2 serving_and_committing=True
[m6-gate] RESULT: PASS
```

Confirmed stable across 3 independent full runs (dip numbers vary run to run
as expected on a shared box: replace-a-box's `add+catchup` dip read 0.0 %,
2.0 %, and 0.0 % across the three runs, and resize-3-5-3's per-step dips
ranged roughly 0–5.3 % — every number stayed comfortably under the 10 % gate
on all three runs).

**Three real bugs were caught and fixed getting to this PASS** — two in the
orchestrator itself, one a genuine scenario-design gap (not a product bug):

1. **Double `bind_addr()` call** (orchestrator bug) — the address registered
   with the cluster via `uc2ctl add-learner --addr` was computed separately
   from the address actually passed to the spawned node's `--bind` flag;
   `LocalHost.bind_addr()` mints a FRESH ephemeral port on every call, so the
   two calls silently picked DIFFERENT ports. The spare's node process
   started and ran perfectly happily — just never received a single byte of
   replication, because the cluster was sending to a port nothing was
   listening on. Fixed by computing the address once and threading it
   explicitly into both call sites (`start_node_m7` now takes `bind_addr` as
   an explicit parameter rather than calling `host.bind_addr()` itself).
2. **A reused host slot's instance directory was never wiped** (orchestrator
   bug) — `replace-a-box` crashes an original voter, freeing its physical host
   slot for `resize-3-5-3`'s own growth; but the freed slot's directory still
   held the OLD id's durable state (`journal/`, `state/config.state` with the
   old id's own `ConfigRecord`/tombstones). Starting a brand-new id on that
   same directory produced a node stuck at a random half-adopted config
   version — a fresh id must never inherit another id's durable state. Fixed
   with `Host.reset_dir()` (wipe + recreate), called immediately before every
   spare host's node process starts. This exact failure mode is now also
   called out in `docs/ops/uc2-runbook.md` §6's decommission section — it is
   a real operational hazard for a fleet operator "replacing a box" too, not
   just a test-harness artifact.
3. **The gate's own write load could outrun a fresh, far-behind learner's
   catch-up** (scenario-design gap, root-caused, not a product bug) — the
   `m7_gate` reference SM deliberately carries **no snapshot capability**
   (M7's scope is membership, not purge/snapshots), so a learner joining after
   real accumulated backlog can ONLY catch up via NAK/journal replay — a
   throughput-bounded path, unlike M6's near-instant snapshot install for a
   below-floor joiner. A standalone repro measured replay throughput at
   roughly **72 KB/s** on this box, while the unthrottled `loadclient` role
   was sustaining roughly **159 KB/s** — a race the learner can never win, not
   a hang (confirmed directly: stopping the writer let the SAME learner catch
   up completely, just slowly). This surfaced as `resize-3-5-3`'s second
   add-learner never converging within budget. Fixed by pacing
   `m7_gate loadclient`'s writer (`thread::sleep(Duration::from_millis(3))`
   after each committed write, capping it well under the observed replay
   ceiling) — this keeps the scenario a genuine "does a learner catch up
   under REAL concurrent load" proof (M7's actual claim) rather than an
   unrelated replay-throughput benchmark (M5's job). **Worth a maintainer's
   attention**: a production M7 deployment without a `SnapshotStateMachine`
   (or with purge disabled) has the same ceiling — a learner joining far
   behind a sufficiently fast writer may never catch up. M6's snapshot
   capability removes this ceiling entirely (below-floor joins install a
   snapshot instead of replaying), so the practical guidance is: **pair M7
   reconfiguration with M6 snapshots/purge in any deployment with meaningful
   sustained write load**, exactly as the M6 gate doc's own SM already does.

All three are disclosed here rather than silently fixed because they are the
kind of finding the M6 precedent already established this doc records: a bug
that would have produced a false negative (or worse, a false positive from a
broken measurement) on the real fleet run — and, for #3, a real operational
caveat worth stating plainly rather than leaving implicit.

## Fleet result — **PASS** (2026-07-14)

Run on a real 5-host AWS fleet (3 voters + 2 spare `c6id.2xlarge` hosts,
us-east-1, one placement group, real cross-host UDP, NVMe journals, fsync on,
**snapshots + purge enabled** — see "what the fleet taught" below), driven by
`bench-infra/scripts/m6_fleet_gate.py --m7 --fleet`, unthrottled write load,
admin ops via real `uc2ctl` over each host's cnc admin slot:

```
[m6-gate] == M7 FLEET (5 hosts) gate ==
[m6-gate] leader elected: node0
[m6-gate] [PASS] replace-a-box — add-learner 10 -> catch-up -> promote -> remove 1
          (new voter set host-ids [0, 2, 10]); per-transition dip:
          add+catchup=0.2%, promote=1.9%, remove=4.7% (gate <10%)
[m6-gate] [PASS] resize-3-5-3 — 3->5 (add+promote 11, add+promote 12) -> 5->3
          (demote+remove 11, demote+remove 12); final voter set == the set this
          scenario started with; per-transition dip: add-11=0.0%, promote-11=1.9%,
          add-12=0.0%, promote-12=0.0%, demote-11=0.6%, remove-11=0.4%,
          demote-12=0.8%, remove-12=0.0% (gate <10%)
[m6-gate] [PASS] leader-self-removal — gap=3.22s (gate <10s), zero
          committed-high-water loss, new leader node10 serving_and_committing=True
[m6-gate] RESULT: PASS
```

| scenario | result | headline number | gate | verdict |
|---|---|---|---|---|
| **replace-a-box** | full live cycle, learner joined via snapshot session | worst transition dip **4.7 %** | dip < 10 %, zero read divergence | ✅ PASS |
| **resize-3-5-3** | 8 transitions, final voter set restored | worst transition dip **1.9 %** | dip < 10 %, zero read divergence | ✅ PASS |
| **leader-self-removal** | step-down at commit-crossing → clean re-election | gap **3.22 s**, zero committed loss | gap < 10 s | ✅ PASS |

### What the fleet taught (the first run was an honest FAIL)

The first fleet run failed all three scenarios — and every failure was the
**gate harness**, not the cluster. The debugging is recorded because it is
the milestone's best evidence that the honest-gates discipline works:

1. **The cluster never stalled.** An on-host sampler (≈390 Hz, 21,878 samples
   across add → promote → demote → remove) found **zero commit stalls
   ≥ 200 ms** during any voter-set transition. The reported 11–16 % "dips"
   were SSH measurement skew: the dip windows bracketed *remote* commit reads
   with *local* wall-clock around ~0.4–0.8 s ssh+sudo round trips. Fix:
   `m7_gate probe --rate-secs N` measures rates entirely on-host; ssh only
   transports the result. Post-fix transition dips: 0.0–4.7 %.
2. **The "divergence" was an exit-code artifact.** `SshHost.unit_exit`
   returned 1 for any non-`active` unit, so normally-stopped (`--collect`ed)
   loadclients read as "exit 1 = divergence". The loadclient logs showed
   perfectly monotonic reads throughout. Fix: divergence is now detected from
   the loadclient's own `DIVERGENCE:` log line.
3. **The 10.30 s self-removal "gap" was polling granularity** (five
   *sequential* ssh probes per iteration). Parallelized, the real gap
   measured **2.7–3.2 s** — the failover class the design predicted.
4. **One genuine operational property, exactly as documented:** with the
   loadclient unthrottled (M6-level load), a snapshot-less joining learner
   was outrun by the writer (stuck at config v1 while the cluster advanced) —
   the runbook's §6 caveat verbatim. The gate now runs the runbook's own
   prescription — **M7 paired with M6 snapshots/purge** — and the joiner
   converges via a snapshot session (which also exercises the M7 snapshot
   config-carry end-to-end at fleet scale, closing that fleet-watch item).
5. A rerun also caught that gate runs must **wipe instance dirs at start**
   (clean room): a rerun inherited a prior experiment's tombstones and
   refused the add-learner — the fresh-forever-id rule enforcing itself.

`v2.1.0` is tagged on this PASS.

---

## v1/v2.0 retirement context

Not reopened by this milestone: v1 retirement and the M5/M6 fleet gates are
already PASSED and recorded in `docs/benchmarks/uc2-m5-gate-2026-07-12.md` and
`docs/benchmarks/uc2-m6-gate-2026-07-12.md`. M7 is purely additive on top of
that baseline — it does not touch the M1–M6 proof stack, and the workspace
still builds and lints clean with M7's code in place (`cargo build
--workspace --all-targets`, `cargo clippy --workspace --all-targets -- -D
warnings`).
