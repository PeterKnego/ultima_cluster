# uc2 M12 gate — gateway kit, admin authentication and audit, packaging and publishing

**Date:** 2026-08-22

> **Living document, filled in row by row.** Created by Task 12 of the M12a
> implementation plan alongside `uc2_gateway/examples/m12_gate.rs`, then
> extended by M12b (row 4) and M12c (rows 5, 6, 7 and the added rows 11–13).
> It carries every row from spec §8, filled with what is known at the time
> each sub-milestone lands — fleet-only rows keep their proposed bars and
> empty result cells until a real fleet run happens, and rows that are BUILT
> but not yet EXERCISED say so instead of being rounded up to PASS, per the
> honest-failure protocol M7/M9/M10/M11 used. Rows 8–10 are M12d's: row 9 is
> PASS, row 8 is BUILT with its first nightly run still pending, and row 10
> (the external review) stays **pending** until that separately scheduled step
> happens. **No number in this document from a local run is a
> gate number** — see "Dev box is not a bench" below and `CLAUDE.md`.
>
> Spec §6's `RELEASES.md` / `docs/releases.md` writeup is deliberately NOT
> part of M12c: it covers all four sub-milestones and lands with the
> `v2.6.0` tag, per `docs/how-to/cut-a-release.md` §1.

Spec: `docs/superpowers/specs/2026-08-22-uc2-m12-adoptable-design.md`, §4
(M12a design), §4.6 (tests and gate), §8 (acceptance gate). Plan:
`docs/superpowers/plans/2026-08-22-uc2-m12a-gateway-kit.md`. Precedent for
this document's shape and honesty conventions:
`docs/benchmarks/uc2-m11-gate-2026-08-20.md`.

## What M12a is

M12a is the first of four M12 sub-milestones (M12a gateway kit → M12b admin
authn/audit → M12c packaging/publishing → M12d posture + review), tagged
together as `v2.6.0` once all four land. M12a ships: the **two-tier
state-machine contract** (`RawStateMachine` bytes-in/bytes-out as the core
trait, today's typed `StateMachine` kept as a blanket adapter —
`docs/reference/state-machine-contract.md`), the **`Sessioned<S>`** exactly-
once session-envelope wrapper, and **`uc2_gateway`/`uc2_remote`** — a TCP
edge (`Edge`) that terminates the framed remote protocol on a node's host and
relays it over the existing shmem `Engine`, plus a pipelined, credit-gated,
redirect-following Rust client (`RemoteClient`). None of this touches
consensus, the wire protocol between nodes, or the cnc page — a cluster with
no gateway running is unchanged.

## What M12b is

M12b (Tasks 1–7 on `uc2/m12b-admin-auth`, HEAD `cca681d`) ships **admin
authentication and audit**: an HMAC-SHA256-signed admin request (a new
64-byte cnc line, `CNC_OFF_ADMIN_AUTH = 3904`), an append-only audit log
(`<instance_dir>/audit.jsonl`, one `fsync`ed JSON line per admin answer,
written before the answer is published), and two explicit-choice config
sections (`[crypto].enabled`, `[admin]`) that turn "off by default" into
"stated by default" — an absent section is now a named startup refusal
rather than silently cleartext/unauthenticated. No wire protocol bump (the
cnc page grows within its existing reserved band; node-to-node datagrams are
unchanged). Row 4 below is M12b's row; every other row is
unaffected by this sub-milestone.

## What M12c is

M12c (Tasks 1–5 on `uc2/m12c-packaging`) ships **packaging, publishing and
hygiene** — the parts of "a stranger can adopt this" that are not code the
cluster runs:

- **Version identity.** Lockstep `2.6.0` across `[workspace.package]`, all
  twelve publishable crates, the git tag, the tarball names and the image
  tag; publish metadata (description/keywords/categories/license/repository)
  on every one; `uc2_sim`, `uc-lincheck` and the example crates explicitly
  `publish = false`. The promise that version number makes is written down in
  `docs/reference/semver-policy.md`.
- **Supply chain.** `deny.toml` + two `cargo-deny` passes in CI; dead
  `quinn`/`rustls`/`tokio`/`futures` deps removed; `snow`'s `std` feature
  dropped so exactly one AES-GCM implementation is in the graph.
- **A stated MSRV.** `rust-version = "1.89"`, a pinned `rust-toolchain.toml`
  (1.96.0), and an `msrv` CI job that runs clippy on the real floor.
- **Release artifacts.** `.github/workflows/release.yml` — native x86_64 and
  aarch64 builds, tarballs + `SHA256SUMS` + a CycloneDX SBOM, cosign keyless
  signing, a GitHub Release, and a signed multi-arch `ghcr.io` image — all
  gated on `release-smoke`, which runs the artifact's own quickstart in a
  bare `ubuntu:24.04` and then the same binaries as a compose stack.
- **A quickstart that needs no toolchain.** `packaging/quickstart-local.sh`
  (three nodes, three services, three gateways, a real election, two
  committed writes and a linearizable read driven from outside by the new
  `counter-remote` example binary), plus `packaging/Dockerfile` and
  `packaging/compose.yml`.
- **Docs.** An artifacts-first `docs/QUICKSTART.md`, install-from-release in
  `run-a-cluster.md` and the runbook, `docs/how-to/cut-a-release.md`, and
  `docs/reference/semver-policy.md`.

M12c changes nothing a running cluster does: no consensus change, no wire
protocol change, no cnc layout change. One product-visible fix rode along
with it (Task 3b): `uc2_remote`'s `request_timeout` is now enforced *while*
the reader is reconnecting, not only once connected — see
`docs/reference/remote-protocol.md` for the documented bound. Rows 5, 6, 7
below are M12c's rows, plus rows 11–13 added for spec §6's hygiene items.

## What M12d is

M12d (Tasks 1–5 on `uc2/m12d-security-posture`) ships **the security posture
and the review-ready package** — no consensus change, no wire-protocol change,
no cnc layout change, and no new runtime surface. Two things landed: a
**fuzzing tier** (a `cargo-fuzz` crate outside the workspace, 14 targets, a
committed deterministic corpus, `scripts/fuzz_smoke.sh`, and `fuzz` +
`fuzz-groups` + `miri` jobs in `nightly.yml`) whose record is
`docs/VERIFICATION.md` §7, and a **security package** (`docs/security/`'s three
documents plus `SECURITY.md` and the README's Security posture / Scope and
limits sections). It also produced three product fixes, listed below. Rows 8,
9 and 10 are M12d's rows.

## Dev box is not a bench

Per `CLAUDE.md` and standing project memory: this box is a shared 4-vCPU dev
sandbox. Every number below produced by a local run is labelled **"smoke
(dev box, 4 vCPU) — not a gate number."** Bars that require a real number are
**fleet-only**, exactly as M5/M6/M7/M8/M10/M11 treated their throughput/cost
bars. A local run failing (or a local ratio looking bad) never moves a bar
and is never reported as the milestone's result.

## The gate (spec §8, every row)

| # | Row | Proof | Status | Evidence |
|---|---|---|---|---|
| 1 | Gateway follows the leader across failover; zero acked writes lost | remote lincheck capstone (`examples/uc2-crashtest/tests/remote_lin.rs`), CI, hard-crash-tests | **pending nightly** (green 3× locally under `--features hard-crash-tests`) | Local: `remote_lin_envelope_on` — Ok 100%, expired 0, acked writes lost 0, 6 leader kills + 6 gateway respawns/run, ~41 s/run, 3 consecutive runs. `remote_lin_envelope_off` asserted under its documented at-least-once contract (re-sent mutations recorded `Indeterminate`, never a hidden second effect). CI adjudication pending the next nightly run with Task 11 merged. |
| 2 | Gateway throughput cost vs direct `Engine` | `m12_gate`, fleet; bar **≥ 0.8× direct-`Engine` responses/s at equal inflight** | **FAIL (honest) — 2026-08-24 fleet run (4× `c6id.2xlarge`, us-east-1 cluster PG), `bench-infra/scripts/m12_fleet_gate.py`, 5 cycles, one process per role per host, both arms vs the SAME leader/generation. Median gateway/direct ratio 0.072 (envelope on, Sessioned) / 0.064 (envelope off, raw); 0 lost both arms. UNDER INVESTIGATION — not a shmem-vs-network floor: the gateway arm is latency-bound (p50 ~41 ms vs direct ~3 ms) with throughput pinned at inflight÷latency (4096/0.041s≈100 k/s) while credits reach the full 4096 and TCP_NODELAY is set both ends, so the ~41 ms is a client/edge cadence artifact, not Nagle or the wire. Two investigation paths open (see below).** | direct ~1.3–1.86 M/s, gateway ~87–101 k/s across all 5 cycles both modes; per-cycle MEASURED-PAIRS-JSON captured in the run log. The ≥0.8× bar compares one pipelined `RemoteClient` over TCP against one shmem `Engine` client; whether that is the right comparison is part of the open investigation. |
| 3 | Codec share on the apply thread at the M5 ladder, raw vs typed | `m5_gate` + `apply-profile`, fleet | **PASS (measurement row, no bar) — 2026-08-24 fleet run, payload 509 B, apply-profile on the service: typed `CountSm` sm_apply=1173 ns/frame (87.7% of apply_cycle_total) vs raw `RawCountSm` sm_apply=14 ns/frame (8.0%) — an ~84× per-frame drop, confirming on real hardware the codec-spike finding that motivated the two-tier contract.** | typed 383 k resp/s vs raw 398 k resp/s at 4096 inflight; both over 22–23 M applied frames. Directionally matches the local A/B (61×) and the isolated codec ladder; the fleet number is sharper because the box is not oversubscribed. |
| 4 | Admin op without a credential refused + audited; with one accepted + audited; replay refused | CI integration test | **PASS** | `uc2_node/tests/admin_auth.rs` (unsigned refused `auth_missing`/20 + audited `actor="unverified"`; signed accepted + audited under the signing key's name; bad tag/expired/unknown-key refused + audited; a captured request replayed verbatim produces no second effect and no second audit record — `a_replayed_request_cannot_be_re_presented`; a capture replayed after a node restart, against a cnc header forged back to the captured `instance_id`, is refused `auth_bad_tag`/21 with the config version unmoved — `a_capture_replayed_after_a_restart_is_refused`, the final review's C1 regression; follower verifies before forwarding, and a forwarded proposal is audited on both nodes), `uc2_node/tests/daemon_refusals.rs` (absent `[crypto]`/`[admin]` → named refusal; a world-readable `[admin]` key file → named refusal — the wrong-length case is `uc2_crypto`'s own unit test, `uc2_crypto/src/admin.rs::load_refuses_a_wrong_length_key_file`), `uc2ctl/tests/admin_auth_bin.rs` (the CLI binary end to end: `gen-admin-key`, sign-and-submit, `audit --tail/--json`). All green locally (`cargo test -p uc2_node --test admin_auth --test daemon_refusals`, `cargo test -p uc2ctl`), and both suites run per-PR in `ci.yml`'s fast `uc2_node` integration step as well as in nightly's `capstones` job — see fact (e) below. |
| 5 | Quickstart cluster from published artifacts with no toolchain | `release-smoke`, CI, every tag | **PASS (2026-08-24, `v2.6.0-rc.1`, run 32686497302 — all seven jobs green on the first real run; residual: the aarch64 binaries are built and shipped but still never executed, manual arm run per `cut-a-release.md` §4 still open)** | `.github/workflows/release.yml` job `release-smoke` (the publish gate: nothing is released or pushed unless it passes) runs `scripts/release_smoke.sh <tarball>`, which unpacks the x86_64 tarball inside a bare `ubuntu:24.04` — no Rust toolchain, no source tree, no curl/jq/ss — and runs `packaging/quickstart-local.sh` out of it; then it builds `packaging/Dockerfile` from that same tarball and brings `packaging/compose.yml` up as three nodes + three services + three gateways, driving `counter-remote` through a gateway to `value=10`. **Proven locally (2026-08-23, dev box):** the tarball ASSEMBLY and LAYOUT — the exact `dist/uc2-<ver>-<target>/{bin,packaging,LICENSE,README-release.md}` the workflow builds — extracted and run through `quickstart-local.sh --bin-dir <extracted>/bin`, `PASS` (`reset -> value=0`, two `add 5`, `get --linearizable -> value=10`), plus `sha256sum -c` on the tarball, and the compose `init` service's rendered `node.toml`/`gateway.toml` fed to the REAL loaders (`uc2-node`/`uc2-gateway` reach a runtime bind/attach error, i.e. every config check passed). **NOT proven locally, first real run = the rc tag:** docker, docker compose, buildx and ghcr are unavailable on the dev box, so the bare-container run, the image build and the compose stack are validated in CI only. **And one gap CI does not close at all:** `release-smoke` runs the x86_64 tarball only — the aarch64 binaries are built natively and packaged, but nothing in CI ever executes them, so this row is claimed for x86_64 and unclaimed for aarch64 until somebody runs the arm tarball on arm hardware (`cut-a-release.md` §4 asks for that at the rc tag). |
| 6 | Artifacts and image verifiable | `cosign verify-blob` / `cosign verify`; keyless, identity-pinned | **PASS (2026-08-24, `v2.6.0-rc.1`: `cosign verify-blob` on the x86_64 tarball and `cosign verify` on `ghcr.io/peterknego/uc2:2.6.0-rc.1` both verified from a consumer seat with the pinned identity; the release published as a prerelease and did not claim Latest)** | Signing lives in `release.yml`'s `release` job (`cosign sign-blob --yes --bundle <f>.sigstore.json` over every tarball, the SBOM archive and `SHA256SUMS`) and its `image` job (`cosign sign --yes --recursive <image>@<digest>` — `--recursive` signs the multi-arch index AND each per-platform manifest under it, so a client that pulls by platform digest still finds a signature), both `id-token: write`-scoped and gated on `release-smoke`. Verification is written out, identity-pinned, in `docs/how-to/cut-a-release.md` §5 and `packaging/README-release.md` (`--certificate-identity-regexp 'https://github.com/PeterKnego/ultima_cluster/.github/workflows/release.yml@refs/tags/v.*' --certificate-oidc-issuer https://token.actions.githubusercontent.com`). **Deliberately NOT proven on the dev box, and not provable there:** keyless signing needs a GitHub OIDC identity, which this box has none of — a local `cosign sign-blob` would either fail or, worse, produce a signature under some *other* identity, which is not the thing this row claims. A `workflow_dispatch` dry run does not exercise it either, though not for that reason: a dispatch run *can* mint an OIDC token; the `release` and `image` jobs are simply guarded on `startsWith(github.ref, 'refs/tags/v') && !inputs.dry_run` and never run. Only a real tag reaches them. The `-rc.1` tag in `cut-a-release.md` §3 exists precisely to close this row before the real tag is spent. |
| 7 | Crates publishable | `cargo publish --dry-run` DAG, CI | **M12c Tasks 1–2: PASS**, with a stated dry-run caveat | `.github/workflows/ci.yml` job `publish-check`, green on every push. Three steps: (i) `scripts/check_publish_metadata.sh` — the crates.io-side metadata rules neither `cargo package` nor `cargo publish --dry-run` ever checks (keyword length ≤ 20 chars and count, category slugs), which is how the original `state-machine-replication` keyword was caught; (ii) `cargo package --no-verify` over **all 12** publishable crates in ONE invocation (`ultima-journal`, `uc_protocol`, `uc2_crypto`, `uc2_log`, `uc2_consensus`, `uc2_net`, `uc2_client`, `uc2_node`, `uc2_service`, `uc2_remote`, `uc2_gateway`, `uc2ctl`) — every path dep rewritten to a registry dep and required to resolve, which is what catches a missing `version =` pin or a stray `publish = false`; (iii) `cargo publish --dry-run` for the **4 dependency-free leaves only** (`ultima-journal`, `uc_protocol`, `uc2_remote`, `uc2_consensus`). **The caveat, stated rather than papered over:** a per-crate dry run of a NON-leaf crate cannot pass before the first publish, because its path deps must resolve against the real registry and nothing in this DAG is on crates.io yet — and this is not merely a bootstrap gap (`uc2_node`'s dev-dependency on `uc2_service` is a genuine dev-only cycle no publish order avoids). Batching all 12 into one `cargo package` is what makes cargo resolve them against each other locally instead. So row 7 claims *packaging*, proven for all 12, plus *publishing*, proven for the 4 leaves; the full 12-crate publish is first exercised for real by the manual, ordered sequence in `docs/how-to/cut-a-release.md` §6. `uc2_sim`, `uc-lincheck`, `counter` and `uc2-crashtest` are `publish = false` and deliberately not in the set. |
| 8 | Decoder fuzz job green | `nightly.yml` `fuzz` | **PASS (2026-08-24: green on TWO real-runner nightlies on `8d6cd55` — scheduled run 32688639872 and dispatched run 32689393859; all four `fuzz` legs, `fuzz-groups`, and `miri` succeeded)** | The tier exists and is locally proven; what has NOT happened is a run on a real GitHub runner, so this row is not claimed as PASS. **Built:** `fuzz/` (a `cargo-fuzz` crate outside the workspace — `exclude = ["fuzz"]` plus its own empty `[workspace]` table, because `libfuzzer-sys` needs nightly and the workspace pins stable at an MSRV floor of 1.89), **14 targets** (`uc_protocol_datagram`, `uc_protocol_log_frame`, `uc_protocol_cnc`, `uc2_remote_frame`, `uc2_crypto_open`, `uc2_crypto_handshake`, `uc2_crypto_group_key`, `uc2_crypto_admin`, `ultima_journal_record`, `ultima_journal_stable_value`, `uc2_service_session`, `uc2_node_toml`, `uc2_gateway_toml`, `uc2_node_http`), a deterministic committed seed corpus generated by the real encoders, `scripts/fuzz_smoke.sh [--min-runs N] [SECS] [TARGET…]`, and two `nightly.yml` jobs: `fuzz-groups` (asserts the four matrix legs' union is EXACTLY the manifest's target set — no target in two legs, none missing, none stale — and emits the matrix itself, so the checked list and the matrix are one object) and `fuzz` (4 legs, 600 s per target, `--min-runs 10000`, `fail-fast: false`, crash artifacts uploaded). **Locally proven (dev box, 2026-08-23):** all 14 targets green under `scripts/fuzz_smoke.sh --min-runs 10000 20` — ~118 M executions in that one 20 s sweep, slowest target `uc2_crypto_handshake` at 91 003 runs (9× the floor at 1/30th of the CI budget, ~273× headroom pro-rata at 600 s); the `fuzz-groups` step executed against the real manifest in one passing and three failing configurations; the `--min-runs` floor demonstrated on both its pass and its fail path. Cumulative local execution during M12d ran to several hundred million inputs, and **three real defects came out of it** — see the M12d facts below. **Not yet proven:** no nightly run has executed either job on a runner, so `cargo install cargo-fuzz` timing, the `fromJSON` matrix expansion and `Swatinem/rust-cache`'s `shared-key: fuzz` behaviour are unexercised. The 60-minute timeout has ~17 minutes of headroom over 40 minutes of fuzzing plus a cold sanitizer build; if a runner is slower, the fix is a fifth leg, not a smaller budget. This row becomes PASS on the first nightly in which the `fuzz` job is green. **Confirmed 2026-08-24**: both the 04:04 scheduled nightly and the 04:16 `workflow_dispatch` nightly on `8d6cd55` ran every `fuzz` leg (600 s/target, `--min-runs 10000`), `fuzz-groups`, and `miri` green on GitHub `ubuntu-latest`/nightly runners — `cargo install cargo-fuzz`, the `fromJSON` matrix expansion, and the `shared-key: fuzz` cache all exercised. The scheduled run's overall failure was an unrelated pre-existing flake in `crashtest-crypto` (`sigkill_mid_config_window`, a config-convergence timeout — passed on the same sha's dispatched run and 8/8 locally with `UC2_CRYPTO=1`), not the `fuzz` job. |
| 9 | Security package present | threat model, attack surface, self-assessment in tree | **M12d Task 5: PASS** | Four files in tree: `docs/security/threat-model.md` (assets, actors, an ASCII trust-boundary diagram with its four boundaries named, a "what is defended, by what" table covering crypto ON and OFF separately, admin, IPC, gateway and the observability endpoint, an out-of-model list, and the four M8 §7 accepted residuals quoted verbatim from their canonical text); `docs/security/attack-surface.md` (one row per parser — 19 of them — with entry `file: function`, who can reach it, whether anything authenticates it, its length/shape guards and its fuzz target; plus bind-address guidance for the three listening ports and the computed command-payload ceiling); `docs/security/self-assessment.md` (scope and method, six numbered findings with severity/status/commit, the accepted-weaknesses table with the *why*, seven ranked areas an external review should focus on, the verification inventory, dependency posture and release integrity); and `SECURITY.md` (supported = `2.6.x` only, GitHub private vulnerability reporting, 7-day acknowledgement, coordinated disclosure, and a "what is already known" list so a documented residual is not re-reported as a defect). `README.md` gained a **Security posture** section ahead of **Scope and limits**, which now states the eight-member cap, one node per instance dir, one state machine per cluster, the **≤ 1344 B (crypto off) / ≤ 1312 B (crypto on)** command payload ceiling with its arithmetic, the shmem-or-gateway attachment rule, the off-by-default choices, and the single-AZ caveat. The fix round added the same exposure guidance to the shipped examples themselves — `packaging/node.example.toml`'s `bind` (with `[crypto].enabled = false` that port trusts the network, and the kind-16 membership filter is address-based and spoofable) and `packaging/gateway.example.toml`'s `listen` (unauthenticated plain TCP: bind privately or front it with your own TLS/authn proxy) — because an operator copying an example reads the comment, not the doc. |
| 10 | External review | separate, user-scheduled | **pending** | — |

### M12c rows beyond spec §8

Spec §6 asks for three hygiene items §8 has no row for. They are recorded
here on the same terms as everything above.

| # | Row | Proof | Status | Evidence |
|---|---|---|---|---|
| 11 | MSRV floor is real and enforced | `msrv` job, CI, every push | **M12c Task 2: PASS** | `rust-version = "1.89"` in `[workspace.package]`. The floor was probed, not guessed: it is `std::fs::File::try_lock_exclusive`/`unlock` (`uc2_node/src/backup.rs`), stable since 1.89.0. `.github/workflows/ci.yml`'s `msrv` job reads the value out of `cargo metadata`, installs *that* toolchain, and runs `cargo +$MSRV clippy --workspace --all-targets --locked -- -D warnings` (`$MSRV` is whatever `rust-version` says — `1.89` today) — clippy, not merely `check`, and with an explicit `+<version>` so the checked-out `rust-toolchain.toml` directory override cannot silently redirect it to the pinned 1.96.0. Two 1.89-only lints were red and are silenced with targeted `#[allow(..., reason = "…")]` rather than portability-losing rewrites (`clippy::ptr_arg` on `exec`'s reserved re-entrant `_work: &mut Vec<Event>` queue; `clippy::unnecessary_cast` on `uc2-node.rs`'s `statvfs` width normalisation). Verified locally on the real 1.89.0 toolchain: `check --workspace --all-targets --locked` green, `test -p uc2_crypto -p uc_protocol --locked` green (132 + 71 passed), `clippy … -D warnings` green. |
| 12 | Supply chain: advisories, licenses, bans | `deny` job, CI, every push | **M12c Task 1: PASS**, one documented ignore | `deny.toml` + two `cargo-deny` passes in `.github/workflows/ci.yml` — one on the plain default-feature graph (`arguments: ""`, because the action's own default is `--all-features` and would otherwise silently be the same pass twice) and one on `--all-features`, so `uc2_service`'s non-default `ultima_db` adapter and its transitive graph are actually resolved. Both run `check advisories licenses bans sources`. `bans` includes the spec's "exactly one AES-GCM implementation in the graph" requirement; dropping `snow`'s `std` feature is what removed `ring` and made that true. Dead workspace deps (`quinn`, `rustls`, `tokio`, `futures`) removed. **One residual, ignored on purpose and written down in `deny.toml`:** RUSTSEC-2025-0141, `bincode 2.0.1` flagged *unmaintained* — a maintenance-status advisory, not a defect, and one with **no patched version to move to** ("Solution: No safe upgrade is available!"). bincode is the wire codec for the cnc page, log records and the remote protocol across the workspace, and the typed state-machine tier's byte-identity promise is defined against it, so replacing it is a wire-format migration rather than a hygiene fix. The `deny.toml` entry carries that reasoning, the date, and the instruction to re-check the ignore whenever a maintained successor appears — recorded as a residual, not silently dropped. |
| 13 | `cargo fmt --check` gate | one-shot reformat + CI gate, **if no long-lived branch is open** (spec §1, §6) | **DEFERRED — the spec's own condition is not met** | Spec §1's decision row makes this conditional: *"One-shot + `--check` gate in M12c only if no long-lived branch is open then; otherwise deferred and said so."* Two long-lived worktrees are open right now — `fix/remaining-flakes` and `worktree-uc2-multi-service` — and a one-shot reformat measured **2 731 hunks** of drift (`cargo fmt --all -- --check | grep -c '^Diff in'`, 2026-08-23), every one of which would become a conflict in both. So it is deferred, and this row is the "said so". **The condition to re-run it, verbatim:** when neither of those two branches is outstanding (merged or abandoned), run `cargo fmt --all` as a single mechanical commit that touches nothing else, then add `cargo fmt --all -- --check` as a step in `ci.yml`'s `test` job. Nothing else in M12c depends on it; the `clippy … -D warnings` gate, which is the one that catches defects rather than whitespace, is already enforced on both the pinned stable and the MSRV floor. |

## Local smoke numbers (Task 12, this document's own evidence)

Command (release build, `~/.cache/cargo-target`):

```
cargo run --release -p uc2_gateway --example m12_gate -- --secs 6 --payload 64 --envelope on
cargo run --release -p uc2_gateway --example m12_gate -- --secs 6 --payload 64 --envelope off
```

Recorded verbatim (SMOKE, dev box, 4 vCPU — **not a gate number**):

```
--envelope on
responses/s ratio     : 0.073  (29011 / 398364)
p50 ratio (gw/direct) : 9.675  (83.296 ms / 8.610 ms)
p99 ratio (gw/direct) : 22.178  (513.802 ms / 23.167 ms)

--envelope off
responses/s ratio     : 0.074  (28387 / 385369)
p50 ratio (gw/direct) : 12.642  (109.052 ms / 8.626 ms)
p99 ratio (gw/direct) : 8.645  (198.574 ms / 22.970 ms)
```

Direct-arm absolute numbers land close to the M5 headline bar's raw figures
(~385k–398k responses/s @ p50 ~8.6 ms on this box) despite this box never
having cleared that bar in its own dev-box smoke history.

**The two arms run ONE AFTER THE OTHER, never concurrently** (`main()` runs
`run_direct_arm()` to completion — including stopping all three of its
nodes and services — before `run_gateway_arm()` starts; `uc2_gateway/examples/m12_gate.rs`).
So the low ratio is not "two clusters splitting one box's cores"; it is two
SEPARATE oversubscription stories, one per arm, on the same shared 4-vCPU
box:

1. **Each arm, on its own, already oversubscribes this box.** The direct
   arm alone is 3 busy-spinning nodes (consensus/sender/receiver/archive
   agents each) + 3 services + the measuring client contending for 4 vCPUs —
   the same non-representative crowding the M5/M11 smoke docs already call
   out, before a gateway is in the picture at all. The gateway arm repeats
   that same 3-node + 3-service crowding and ADDS three more things
   competing for the same 4 cores: 3 edges (each an acceptor thread + a
   driver thread), the `RemoteClient`'s reader thread, and this harness's
   own 8-thread waiter pool. That is strictly more concurrent work on the
   same core count, which alone would depress the gateway arm's numbers
   relative to the direct arm even if the gateway added zero real cost.
2. **On top of the oversubscription, the gap is also architecturally real —
   see fact (d) below.** A single edge's driver thread writes every response
   for every connection sequentially (`edge.rs`'s "Head-of-line blocking"
   section), and `RemoteClient` funnels every submit/response through one
   `Mutex<State>` (`client.rs`'s "One lock, one order"). Both are genuine,
   documented single-writer constraints that will still cost something on a
   fleet host with a core to spare; the fleet run is what states how much of
   the local ratio survives once the oversubscription confound above is
   removed.
3. This harness's own waiter pool (8 threads, each with its own histogram —
   no shared lock on the recording hot path, fixed during this task after an
   initial run showed a `Mutex<Histogram>` contention artifact) is not
   itself the bottleneck: the credit-gated pipeline (per-connection window
   4096, matching `--inflight`) queues far deeper than 8, and the measured
   p50/p99 track queueing depth against throughput, not per-request network
   RTT.

## Facts the gate must state (spec §4.6 / design notes, binding across every
future fleet re-run of row 2)

a. **With the raw contract, every replica — followers and the replay path,
   not just the leader — runs the typed adapter's response encode.** Only
   `publish` (making the response visible to a client) is leader-gated; the
   apply-side decode/encode cost in `docs/notes/2026-08-22-codec-budget-spike.md`
   is paid on every node, every commit.
b. **`credits.rs` (`uc2_gateway/tests/credits.rs`) proves the per-connection
   credit ceiling over the wire.** The AIMD descent (the credit squeeze on
   `Backpressure`) is covered by `conn.rs`'s unit tests only — there is no
   integration test today that drives a real `Backpressure` condition over
   the wire and observes the halve-then-climb-back behavior end to end.
c. **The remote lincheck capstone (row 1) is `submit` → `wait` at
   concurrency 4** (one request in flight per worker, by construction — an
   op must be a single interval to be a linearizability history entry at
   all). The SDK's *pipelining* (many outstanding requests per connection)
   is exercised by `failover.rs`'s 200-write scripted run and by this
   document's own `m12_gate` throughput harness, not by the capstone.
d. **The single driver thread per edge serializes outbound writes.** A
   stalled client's write (up to `WRITE_TIMEOUT` = 1 s) can delay every other
   client's responses on that edge — `edge.rs`'s "Head-of-line blocking"
   section documents the mechanism and its consequence (a stalled peer can
   make another client see `UNKNOWN`, resolved by re-send under the session
   envelope). Any fleet cost number for row 2 includes this cost; it is not
   an artifact to be tuned away before reporting.
e. **Commands are MTU-bounded to ~1.3 KB.** `uc_protocol::v2::datagram::MTU_DEFAULT`
   = 1408 B is not operator-configurable; a raw command payload above that
   cannot fit one datagram (`Sender::new`'s hard assert) regardless of
   transport.
f. **`SessionConfig` (`window`, `max_clients`, `max_bytes`) is part of the
   replicated contract**, exactly like `apply` itself: every replica running
   `Sessioned<S>` must run with identical `SessionConfig` values, and
   `install_snapshot` refuses a snapshot whose embedded config disagrees with
   the live node's rather than silently retuning it (`session.rs`'s module
   doc).
g. **The two arms' completion-timestamp mechanisms are not identical, a
   small asymmetry inherited from `m5_gate`.** The direct arm's matcher
   thread polls in a loop with a 20 µs idle-sleep floor between empty polls
   (`run_client_measurement`'s poll loop), so a response's recorded
   timestamp can lag its real arrival by up to ~20 µs; the gateway arm's
   waiter threads instead block on `Ticket::wait_timeout`, so a response is
   timestamped as soon as the OS wakes the blocked thread. Neither biases
   the ratio in the gateway's favor, but it means the two arms' latency
   numbers are not measuring completion detection the same way.

## M12b facts (binding on row 4, and on any future reader of this doc)

a. **One `fsync` per admin op, on the consensus thread, not the hot path.**
   `AuditLog::record` (`uc2_node/src/audit.rs`) does one `write_all` plus one
   `sync_data` per admin decision, and every answer site (the local cnc
   response line, the kind-17 forwarded-config reply) calls it before
   publishing. **One carve-out, added by the final review (I4):** a
   byte-identical re-send of an already-answered, already-recorded proposal
   — same nonce, served from the leader's dedup cache — is counted
   (`config_proposal_dedup_resend`), not re-recorded, because it repeats an
   answer this file already holds rather than being a new admin event. That
   is what bounds the fsync half of fact (c) below; every request that
   produces a fresh decision is still recorded before that decision is
   published. Admin operations are operator-rate — tens a year on a busy
   cluster — so this cost (a few ms on ordinary storage) is paid at most a
   few times a year, never inside `apply`/replication/archive's per-cycle
   loop. If the write or the `fsync` fails, the request is refused
   (`REASON_AUDIT_FAILED = 24`) rather than answered unrecorded — see
   `uc2_node/src/node.rs`'s `audit_admin`.
b. **No `(seq, nonce)` replay ring, a deliberate deviation from spec §5.2's
   sketch.** The HMAC tag covers `seq`; the consensus agent only ever acts
   on `seq > last_admin_seq`, so a captured request cannot be re-presented
   at its original `seq` (never even read again) and re-presenting it at a
   higher `seq` invalidates the tag. Across a node restart `last_admin_seq`
   resets to 0, but the tag also covers `instance_id`, which changes on
   every restart. A ring would therefore never refuse anything these two
   checks do not already refuse; `expiry_ns` is what bounds the window for a
   live, correctly-sequenced request that is delayed and only then applied.

   **The `instance_id`/`app_id` the tag is bound to come from the node's own
   boot-time state, not from the cnc page** (`Consensus::admin_instance_id`
   / `admin_app_id`, set in `Node::start_with` from the same values it
   writes into `CncMeta`). The final review found this the other way round —
   `verify_admin` read `self.cnc.meta()` per request — which made the
   restart half of the argument above false: the cnc page is a file in the
   instance directory, `read_cnc_header` validates only its magic, so an
   actor with directory write access and NO admin key could capture a signed
   `(auth, req)` pair, await or induce a restart (resetting
   `last_admin_seq` to 0), `pwrite` the captured `instance_id` back into
   `CNC_OFF_INSTANCE_LO/HI`, re-present the captured lines, and have the
   change applied a second time. Fixed, and pinned by
   `uc2_node/tests/admin_auth.rs::a_capture_replayed_after_a_restart_is_refused`,
   which performs exactly that forgery and asserts reason 21 with the config
   version unmoved. (Anti-vacuity: with the binding reverted to
   `cnc.meta()`, that test's replay verifies and reaches `propose_config`,
   coming back as reason 5 `AlreadyPresent` rather than 21.)
   See `verify_admin`'s doc comment (`uc2_node/src/node.rs`) and
   `uc2_node/tests/admin_auth.rs::a_replayed_request_cannot_be_re_presented`,
   and the spec's own "As built" amendment (§5.2).
c. **The kind-16 residual: admin auth is cluster-wide only with wire crypto
   on.** A follower forwards an authenticated request to the leader as a
   `ConfigProposal` datagram (wire kind 16) over the node-to-node UDP
   socket, not the admin band. `on_config_proposal`'s membership guard drops
   a datagram whose source address resolves to no current member before any
   work runs, but with `[crypto].enabled = false` a network-path adversary
   who can send UDP claiming a member's address can still inject a
   proposal — `[admin] auth = "hmac"` authenticates cluster-wide only paired
   with `[crypto].enabled = true`. Stated in
   `docs/reference/configuration.md#admin-authentication`,
   `docs/how-to/change-cluster-membership.md`,
   `docs/how-to/encrypt-node-traffic.md`, and `README.md`'s scope section.

   **The audit-`fsync` half of that residual, bounded (final review, I4).**
   Every kind-16 datagram past the membership guard used to write an audit
   record — including the nonce-dedup re-answer path, so one captured
   datagram re-sent in a loop drove one `fsync` per packet on the consensus
   thread. `on_config_proposal` now re-sends the cached reply WITHOUT a
   second record (the original answer was recorded; a byte-identical
   re-answer of the same nonce is not a new admin event) and counts it as
   `config_proposal_dedup_resend`; unit test
   `uc2_node/src/node.rs::a_dedup_re_send_is_counted_not_re_audited`. The
   remaining residual is stated rather than closed: a flood of FRESH nonces
   from a member — or, with `[crypto].enabled = false`, from anything that
   can spoof a member's source address — still costs one `fsync` each. It is
   bounded by the membership guard, and with `[crypto].enabled = true` by
   peer authentication, which is the same pairing the paragraph above
   states.
d. **`AdminPolicy` is not a `NodeConfig` field.** It lives on
   `StartOpts::admin`, alongside the pre-bound socket — both are live
   process resources, not values a `Clone`-able TOML-mirroring config struct
   should carry. `Node::start`/`Node::start_with_socket` (library callers,
   every in-process test and gate) get `StartOpts::default()` =
   `AdminPolicy::Filesystem`, byte-for-byte the pre-M12b posture; only the
   `uc2-node` daemon (`uc2_node/src/bin/uc2-node.rs`) builds `AdminPolicy`
   from `[admin]` and calls `Node::start_with`.
e. **`admin_auth.rs`/`daemon_refusals.rs` run in the fast per-PR job.**
   `.github/workflows/ci.yml`'s "Tests (uc2_node fast integration)" step
   lists `--test admin_auth --test daemon_refusals` alongside
   `smoke`/`failover`/`learner`/`purge_safety`/`query_barrier`, so the admin
   authn+audit net is a per-PR gate, not a nightly-only one. (It was
   nightly-only when Tasks 1–3 landed — the `--test` list simply was not
   extended — and the final review fixed that.) The two suites are 10 + 3
   tests, ~4.6 s total on this box, well inside the fast job's budget.
   `uc2ctl/tests/admin_auth_bin.rs` is covered per-PR too, via `cargo test
   --workspace --exclude uc2_node` (`ci.yml`'s second test step, which does
   not filter by test file the way the `uc2_node` step does).

## M12c facts: what is validated locally, and what is CI-only

The honest split, because rows 5 and 6 turn on it. Everything in the first
list was actually run on this dev box on 2026-08-23; everything in the second
is written and reviewed but has never executed anywhere, and the first real
run of it is the `v2.6.0-rc.1` tag the user pushes.

**Validated locally (dev box, 2026-08-23):**

a. `packaging/quickstart-local.sh` end to end against a release-profile bin
   directory — three `uc2-node`s, an election won by node 0 in term 1, three
   `counter-service`s, three `uc2-gateway`s, then `reset` → `value=0`,
   `add 5` → `value=5`, `add 5` → `value=10`, `get --linearizable` →
   `value=10`, `PASS`, exit 0. The transcript in `docs/QUICKSTART.md` §2 is
   that run, verbatim.
b. The **tarball assembly and layout** — the exact
   `dist/uc2-<ver>-<target>/{bin,packaging,LICENSE,README-release.md}` tree
   `release.yml` builds — extracted and driven through
   `quickstart-local.sh --bin-dir <extracted>/bin`, plus `sha256sum -c` on
   the tarball (Task 4).
c. The compose `init` service's **rendered `node.toml`/`gateway.toml` fed to
   the real loaders** (`uc2-node`/`uc2-gateway` reach a runtime bind/attach
   error, i.e. every config check passed) — Task 4.
d. `publish-check`'s first two steps, both green:
   `scripts/check_publish_metadata.sh` (`ok: publish metadata (keywords,
   categories) within crates.io limits`) and the batched
   `cargo package --no-verify` over all 12 publishable crates (exit 0; each
   crate reports its packaged file count).
e. `cargo deny --all-features check` (Task 1 fix round) and the MSRV
   toolchain probe — `cargo +1.89.0 check --workspace --all-targets
   --locked`, `cargo +1.89.0 test -p uc2_crypto -p uc_protocol --locked`
   (132 + 71 passed), and `cargo +1.89.0 clippy --workspace --all-targets
   --locked -- -D warnings` — all green after the two targeted `allow`s.
f. `cargo doc --workspace --no-deps --lib` (the shape `docs.yml` builds) and
   `cargo clippy --workspace --all-targets -- -D warnings` after the Task 5
   documentation sweep.

**CI-only, or unproven until the first `-rc` tag:**

g. **Docker, buildx, docker compose and ghcr are unavailable on this box.**
   So the `release-smoke` bare-container run, the `packaging/Dockerfile`
   image build, the three-node compose stack and the ghcr push are validated
   in CI only. `scripts/release_smoke.sh` itself has never been executed
   here; only `bash -n` and `shellcheck` have.
h. **Keyless cosign signing is not provable here and deliberately was not
   attempted.** It needs a GitHub OIDC identity this box does not have; a
   local `cosign sign-blob` would either fail or, worse, produce a signature
   under some *other* identity, which is not what row 6 claims. A
   `workflow_dispatch` dry run does not reach it either — the `release` and
   `image` jobs are guarded on `startsWith(github.ref, 'refs/tags/v') &&
   !inputs.dry_run`. Only a real tag does.
i. **aarch64 binaries are built but never executed.** `release-smoke` runs
   the x86_64 tarball only, so row 5 is claimed for x86_64 and unclaimed for
   aarch64 until somebody runs the arm tarball on arm hardware —
   `docs/how-to/cut-a-release.md` §4 asks for exactly that at the rc tag.
j. **The 12-crate crates.io publish has never run.** Row 7 proves packaging
   for all 12 and `--dry-run` for the 4 leaves; the real publish is the
   manual ordered sequence in `cut-a-release.md` §6, first exercised at the
   tag.
k. **`cargo fmt` is deferred**, with the condition to re-run it stated in
   row 13.

## M12d facts (binding on rows 8 and 9, and on any future reader)

**The fuzz tier found three real defects, and one about itself.** Numbering
matches `docs/security/self-assessment.md` §2.

a. **F1 — five caller-guarded datagram readers panicked on short slices**
   (`read_datagram_header`, `read_request_vote_body`, `read_vote_body`,
   `read_nak_body`, `read_status_body`). **Never reachable through the
   receiver** — every caller did guard — but the totality of the first code an
   unauthenticated UDP packet reaches held only by the discipline of five call
   sites. All five now return `Option`; the pre-guards were kept, so the real
   path is byte-for-byte unchanged (`112b81f`).
b. **F2 — `Sessioned::apply` violated the `out`-is-cleared contract it was
   itself a caller of.** User-reachable and a fail-stop: a contract-abiding
   inner state machine that starts with `out.clear()` truncated the session tag
   away and the slice panicked **on the apply thread**, killing the service on
   its first command. Fixed with a bytes-asserting regression test; the fuzz
   target's inner SM keeps its `out.clear()` so the fix stays guarded
   (`7c908b1`).
c. **F3 — `Sessioned::install_snapshot` pre-allocated up to 1 GiB from an
   unvalidated 8-byte length** before reading a single blob byte, using a sanity
   bound as an instruction. Bounded with `take()`; 20 000 executions went
   91.8 s → 0.34 s (`7c908b1`).
d. **F6 (the harness finding).** Four of the fourteen targets were executing ~16
   inputs per 60 s run while printing a clean line — `llvm-symbolizer` needed
   ~90 s to index one 27 MB sanitizer binary for a single address.
   `-print_funcs=0` fixed it (400 runs: 90 180 ms → 57 ms). **A fuzz tier can
   be green and vacuous**, which is why `--min-runs` exists and why CI asserts
   `--min-runs 10000` against a 600 s budget (`736c1f3`). Green now means
   fuzzed.

**Seams that had to be exposed to fuzz the real code, and their posture.**
`uc2_node::config_file::parse_str` and `uc2_gateway::config_file::parse_str`
are ordinary public API (the file loaders' pure inner half — `load_from_path`
is `read_to_string` + `parse_str`). `uc2_node::obs::http::route_raw` and
`ObsSources::for_tests` are `#[cfg(any(test, fuzzing))]` and **absent from a
shipped build**; `uc2_node/Cargo.toml` declares `check-cfg = ['cfg(fuzzing)']`
so `clippy -D warnings` stays clean without promoting the seam to a Cargo
feature (which would have made it API). `ultima_journal::fuzz_seams` stays
`pub` — a separate compilation unit cannot see `pub(crate)` — but is
`#[doc(hidden)]`.

**What Miri ran, and what it cannot.** The nightly `miri` job runs the *pure*
decoders: `uc_protocol`'s `v2::` wire/cnc/ipc layer and `version` packing (43
tests) and `ultima_journal`'s segment and `stable_value` record decoders (19
tests). All 62 pass **with isolation left on** — nothing needed excluding and
`-Zmiri-disable-isolation` is never used. **The IPC ring buffers are not
Miri-checked and cannot be**, and each of the three blockers was reproduced
rather than assumed: with isolation on, ``unsupported operation: `open` not
available when isolation is enabled``; with isolation disabled, ``unsupported
operation: unsupported flags for `fallocate` in `mode` argument: 16``
(`FALLOC_FL_ZERO_RANGE`, the M11 block-reservation fix); and past both — proved
with a throwaway crate mapping a real file — `unsupported operation: Miri does
not support file-backed memory mappings`. The spec's stated fallback, a
`Vec`-backed ring variant, **was deliberately not built**: it would check a
different object than the one that ships. That gap is stated again in
`docs/VERIFICATION.md` §11.

**What has never run.** Neither `fuzz` nor `miri` has executed on a GitHub
runner (row 8). The YAML parses, every `run:` block is `bash -n`-clean, the
group-check step and both Miri commands were executed locally, and the fuzz
step invokes the same script the local smoke run does — but `cargo install
cargo-fuzz` timing, the `fromJSON` matrix expansion and the fuzz build cache
are unexercised until the first nightly.

**`--min-runs 10000` is a stall floor, not a coverage bar.** The slowest
healthy target on this box — `uc2_crypto_handshake` — is ~273× the floor
pro-rata at the 600 s CI budget (row 8 states the same number). It catches a
symbolizer-class stall; it does not catch a target that has merely become 100×
slower. A tighter per-target bar needs per-target numbers from a runner, which
do not exist yet.

**Two things the package documents rather than fixes, both surfaced by the
Task 5 review.** (i) **A malformed query frame fail-stops a typed state
machine, pre-commit, from an unauthenticated client**: `uc2_service`'s blanket
`RawStateMachine` impl decodes a query with `.expect("corrupt query frame
(fail-stop)")` and `apply.rs`'s query branch calls it **while holding the SM
`Mutex`**, so one bad `QUERY` body through a gateway panics the apply thread
and poisons the lock — no quorum, no leadership, no commit involved. Changing
that decode's error semantics is a design decision (the same `.expect` guards
the post-commit apply path, where fail-stop *is* right), so it is **parked as a
follow-up**; the raw tier is the workaround, and it is written up in the attack
surface, the threat model and the self-assessment's known-weakness table.
(ii) **The `uc_protocol::ring` buffers have no interleaving or UB coverage at
all** — a claim in an earlier draft that loom covered them was wrong and was
corrected everywhere, `docs/VERIFICATION.md` §7 and §11 included. The tree's
one loom model (`uc2_log/tests/loom_frame.rs`) checks the **log buffer's**
frame-visibility protocol, not the ring code; offset-pin tests freeze the
rings' layout and nothing checks the MPSC claim-then-commit sequence or the
broadcast seqlock. It is now a named item in the self-assessment's "focus here"
list.

**The security package is documentation, and row 9 claims exactly that.** No
part of row 9 asserts that the system is secure — it asserts that what is
defended, what is not, and what was found are written down where a reviewer
can check them. Row 10 (the external review) is the row that would say more,
and it is still pending.

## Links

- Codec budget spike: `docs/notes/2026-08-22-codec-budget-spike.md`
- State-machine contract reference: `docs/reference/state-machine-contract.md`
- M11 gate doc (format precedent): `docs/benchmarks/uc2-m11-gate-2026-08-20.md`
- M12 design spec (M12a §4, M12b §5 incl. the "As built" amendment): `docs/superpowers/specs/2026-08-22-uc2-m12-adoptable-design.md`
- Gateway harness: `uc2_gateway/examples/m12_gate.rs`
- Admin authentication reference: `docs/reference/configuration.md#admin-authentication`
- Semver policy (M12c): `docs/reference/semver-policy.md`
- Cut a release (M12c): `docs/how-to/cut-a-release.md`
- Release tarball README: `packaging/README-release.md`
- Quickstart (artifacts-first): `docs/QUICKSTART.md`
- Security package (M12d): `docs/security/threat-model.md`, `docs/security/attack-surface.md`, `docs/security/self-assessment.md`, `SECURITY.md`
- Fuzzing tier (M12d): `fuzz/README.md`, `scripts/fuzz_smoke.sh`, `docs/VERIFICATION.md` §7
- Change cluster membership (the `--admin-key`/`gen-admin-key`/`audit` flow): `docs/how-to/change-cluster-membership.md`
