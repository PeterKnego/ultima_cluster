# Attack surface

Every place `ultima_cluster` parses bytes it did not write, what reaches it,
what bounds it, and which fuzz target covers it. The
[threat model](threat-model.md) says who the adversaries are; this page is the
inventory. [Self-assessment](self-assessment.md) records what was found in it.

"Fuzz target" names a real target in [`fuzz/fuzz_targets/`](/fuzz) — there are
eighteen (the three newest, `uc_protocol_timer_frame`,
`uc_protocol_sched_record` and `uc_protocol_schedule_table`, arrived with log
time and timers, 2.11.0),
each run for 600 s per night with an asserted execution floor
([VERIFICATION §7](/docs/VERIFICATION.md#7-fuzzing-decoders-total-on-untrusted-bytes),
[`fuzz/README.md`](/fuzz/README.md)).

---

## 1. The inventory

| Surface | Entry (file: function) | Reachable by | Authenticated? | Length / shape guards | Fuzz target | Notes |
|---|---|---|---|---|---|---|
| **UDP datagram header** | `uc_protocol/src/v2/datagram.rs: read_datagram_header`, via `uc_net/src/receiver.rs: Receiver::poll` → `crypto_admit` → `on_datagram` | anyone who can send a packet to the node's UDP port | crypto ON: yes, AEAD with the header as AAD. crypto OFF: **no** | `n >= DATAGRAM_HEADER_LEN` pre-guard *and* the reader is total (returns `Option`) | `uc_protocol_datagram` | The first code an unauthenticated packet reaches. With crypto off it is also the last line of defence. |
| **Datagram body readers** (kinds 3, 4, 5, 7, 8, 9, 10, 12, 14, 16, 17) | same file: `read_nak_body`, `read_status_body`, `read_append_position_body`, `read_request_vote_body`, `read_vote_body`, `read_term_map_body`, `read_read_probe_body`, `read_snap_begin_body`, `read_snap_nak_body`, `read_config_proposal_body`, `read_config_reply_body` | as above | as above | each checks its own fixed `*_BODY_LEN` and returns `Option`; `read_term_map_body` additionally caps at `MAX_TERM_MAP_WIRE_ENTRIES = 64` and against the caller's `out` slice | `uc_protocol_datagram` | **Four of the five** readers that M12d made total live in this row — `read_request_vote_body`, `read_vote_body`, `read_nak_body`, `read_status_body`; the fifth, `read_datagram_header`, is the row above. See finding F1 in [the self-assessment §2](self-assessment.md#2-findings). |
| **`AppendPositionBody`** (wire 0.5.0 content attestation) | `datagram.rs: read_append_position_body` (`APPEND_POSITION_BODY_LEN = 8`) | any peer, or anyone spoofing one with crypto off | crypto ON only | fixed 8-byte body; a header-only (0.4.0) report reads as **unattested and is not counted** | `uc_protocol_datagram` | The term the sender attributes to the byte below its reported position. A disagreeing report is declined by the leader — the field turns commit ranking from a position quorum into a content quorum. |
| **Snapshot-session framing** (`SNAP_BEGIN` 12, `SNAP_CHUNK` 13, `SNAP_NAK` 14, `SNAP_DONE` 15) | `datagram.rs: read_snap_begin_body` (`SNAP_BEGIN_FIXED_LEN = 120`, wire 0.7.0: per-row `identity`/`version` arrays, one entry per declared FSM, compared positionally against the receiver's own), `read_snap_nak_body`, `receiver.rs: FollowerReceiver::snap_begin` (the positional comparison itself) | any peer | crypto ON: yes — **all** SNAP kinds are sealed since M8 T17 | fixed 120-byte body, no tail (`SnapBeginBody.config` retired with layout V4, cluster-FSM spec §5.6); total readers (any `layout` value decodes, and trailing bytes past the fixed part are ignored rather than refused — the receiver decides, by name). Three named refusals on the receiving node: `peer wire ≤ 0.6.0` (`layout != SNAP_BEGIN_LAYOUT_V4` — a wire-0.6.0 `SNAP_BEGIN_LAYOUT_V2` body, or the intermediate 0.7.0 `SNAP_BEGIN_LAYOUT_V3` shape that carried a trailing config, whose first 120 bytes decode identically so nothing but this discriminator separates them; or a body shorter than 120 bytes, which is what a genuine 0.6.0 sender's 34-byte-plus-config body is, counted under the same refusal so a flag-day mix is never silent), `identity mismatch` (the two sides' identity arrays disagree at some row, compared positionally by name hash — or the artifact's row is outside the sender's own declared mask despite the arrays agreeing everywhere, counted under the same refusal but recorded as the distinct `RefusalKind::ArtifactId`), and `version mismatch` (both sides' packed version at the disagreeing row is nonzero and the two differ) | `uc_protocol_datagram` | A forged `SNAP_BEGIN` would carry attacker-chosen application state into a joining node — and, since the cluster FSM, its **membership, schedule table and settings** too, no longer on this body but as one more artifact in the same session under the reserved `service_id = 255`, installed by fiat before the joiner's purge floor advances. The M8 fall-through that closes it is pinned by `an_unsealed_snap_begin_is_refused_now_that_t17_landed`. Since 0.6.0 a session carries one artifact per declared id, so a forged `SNAP_BEGIN` (crypto off) can populate **N** `snapshots/<id>/` directories on the joiner, not one — plus `snapshots/cluster/`. `service_id` is a bare, peer-controlled `u8` (0..=255, never bounds-checked on the wire) and is NEVER used to index `identity`/`own_identity` directly: the declared-mask bit test uses `checked_shl` (an id ≥ 64 folds to an always-false bit test, not shift UB), and the row recorded for diagnostics comes from a `find` over the receiver's own local array, read back with `.get().copied().unwrap_or(0)` — never a raw index. The regression `an_out_of_range_service_id_is_refused_not_a_panic` drives a forged out-of-range-`service_id` `SNAP_BEGIN` and then a subsequent VALID session over the same link, proving both the refusal and that the receiver agent survives it rather than panicking on an out-of-bounds index. |
| **Snapshot PULL framing** (`SNAP_REQUEST` 22, `SNAP_REDIRECT` 23, wire 0.7.0, 2.11.0 — the standby return path, coordinated-snapshot spec §5.7) | `uc_protocol/src/v2/datagram.rs: read_snap_request_body` (`SNAP_REQUEST_BODY_LEN = 12`) and `read_snap_redirect_body` (`SNAP_REDIRECT_BODY_LEN = 16`), routed by `uc_net/src/receiver.rs: on_datagram` to `uc_net/src/sender.rs: Sender::on_snap_request` and `uc_node/src/node.rs: Consensus::follow_snap_redirect` | any peer, or anyone spoofing one with crypto off | crypto ON: yes — both kinds are `Scope::Pairwise` (`uc_crypto/src/transport.rs: scope_of`), so an unsealed datagram never reaches either handler | fixed-length, total decoders (`Some` only at the exact length; short and long both `None`, pinned by the round-trip/short/long tests beside them). `SNAP_REQUEST` is additionally gated on `from` being a member of the CURRENT membership (voters **and** learners, minus self — `Sender::set_members` / `CtrlMsg::SetPeers`'s `members`); a non-member is a named, counted drop, `SenderStats::snap_request_unknown_peer`. One session at a time per node, 30 s idle timeout | `uc_protocol_datagram` | **Why `SNAP_REQUEST` needs the member gate.** It is the one snapshot path deliberately NOT leader-gated — the intended source is a learner, which is never the leader — where the below-floor NAK path returns before `serve_nak` on a follower (`sender.rs`, the leader-role gate). Without the gate, crypto off, every node is a UDP reflector: a 28-byte spoofed datagram elicits a whole snapshot set to an address of the attacker's choosing. That is a harm to a THIRD party, which the standing crypto-off residual ("a network-path adversary already owns the cluster") does not cover, so it is closed in code rather than accepted; it was naturally rate-limited (one session per node) but the amplification factor in bytes is unbounded. Watched red by `a_snap_request_from_an_address_outside_the_membership_opens_nothing`. **`SNAP_REDIRECT` is graded lower and is NOT gated.** The worst a forged one can do is make a below-floor joiner ask a real cluster member for a snapshot set: `follow_snap_redirect` resolves `learner_id` against the joiner's own committed membership and drops an unknown id by name, the position it names is refused above this node's own durable frontier at three independent layers, and the set that arrives is verified by `SNAP_BEGIN`'s identity/version comparison exactly as any other session's. So it is a nuisance (a wasted pull), not a state-injection vector. |
| **Snapshot artifact envelope** (2.11.0, ruling P6) | `uc_service/src/snapshots.rs: decode_snapshot_envelope` / `verify_snapshot_envelope` (`SNAPSHOT_ENVELOPE_LEN = 16`), reached from every install path: `uc_service/src/replay.rs` (reconstruction and the joiner) and `uc_node/src/backup.rs` (`uc2ctl verify-backup`) | a peer, INDIRECTLY: bytes that arrived over a snapshot session are written to `snapshots/<row>/` and decoded on install. Also anything with write access to the instance directory, or to a backup archive | no — the envelope is an INTEGRITY tag, not an authenticator; the datagrams that carried the bytes are what is authenticated (crypto ON) | pure, total decoder over a fixed 16 bytes: `Short(n)` below 16, `BadMagic` on a wrong magic, `Mistagged { built, presented }` when the recorded position is not the one the caller is installing at; a trailing payload is left untouched for the state machine | `uc_service_snapshot_envelope` | The tag is an **exclusive** frontier, so an artifact built at an earlier position and renamed passes any check a state machine could write — the reason this lives in the framework and has exactly ONE writer (`SnapshotStore::publish`). Every install verifies, each a named refusal. Its threat value is against a MISTAKE (a restored backup, a hand-copied file, a renamed artifact) rather than against a forger: an attacker who can write the instance directory can write a correct envelope too, and is already inside the compromised-host case the threat model excludes. |
| **`CLUSTER` frame body** (frame type 4, wire 0.7.0, 2.11.0 — the cluster FSM's commands: membership, the schedule table, the settings record) | `uc_protocol/src/v2/frame.rs: read_cluster_prefix` (`CLUSTER_BODY_PREFIX_LEN = 8`), then the kind's own decoder — `config::decode_config`, `schedule::decode_schedule_table` or `settings::decode_settings` — reached from `uc_node/src/cluster_agent.rs` on every node at commit, from the archive's header walk (kind 1 only, feeding the consensus kernel at durability), and from the leader's `schedule apply` / `settings apply` paths over a staged file | a peer's replication stream (as a log frame), and a local writer of the instance directory (as a staged file) | inherits the datagram plane's answer on the wire; the staged files are digest-checked (rows below) | **total on any input**: `read_cluster_prefix` returns `None` on a short body, an unknown kind byte, or a **non-zero reserved byte** (so the seven reserved bytes stay claimable without ambiguity), and each kind's payload decoder is itself total — `decode_settings` refuses any length but `SETTINGS_LEN = 29`, an unknown encoding version, or an unknown `snapshot_target` byte; `decode_schedule_table` keeps every guard listed in the row below | `uc_protocol_cluster_frame` (the prefix plus all three payload decoders) and `uc_protocol_settings` (the settings record alone, with a re-encode round-trip) | A forged `CLUSTER` frame (crypto off) can change the cluster's membership, schedule or settings — the same class of exposure a forged `CONFIG` frame had since M7, and the same answer: turn on wire crypto. Acceptance is decided **in `apply`, on FSM state only** (`ClusterFsm::validate`), so every replica reaches the identical verdict; the leader's pre-append answer runs that same function against a clone of the committed view rather than a parallel implementation. Node-local bounds are **clamped at use**, never refused, so a settings record cannot be used to make one host refuse what its peers accept. A body that reaches the archive and does not decode is a fail-stop on every node that recorded it — the M7 CONFIG rule, unchanged. |
| **Noise `IK` handshake** (kinds 18 `HS_INIT`, 19 `HS_RESP`) | `uc_crypto/src/handshake.rs: Peers::on_message`, routed by `receiver.rs: crypto_admit`'s `Scope::Unsealed` arm | anyone who can reach the UDP port, when crypto is **on** | **no — this is the pre-auth surface**; it is what creates a session | `snow`'s own message-length checks; the allowlist rejects an unknown static; claims-our-own-id is an explicit early refusal; an allowlist-reload rate limit driven by `now_ns` | `uc_crypto_handshake` | The most valuable target in the set: turning crypto on *adds* this parser to the pre-auth surface. |
| **AEAD envelope** | `uc_crypto/src/seal.rs: open_in_place`, `open_detached` | any sender of a sealed datagram | this **is** the authentication; the framing arithmetic runs before the tag is verified | `buf.len() >= DATAGRAM_HEADER_LEN + CRYPTO_OVERHEAD` (24 = 8-byte counter + 16-byte tag) before any split | `uc_crypto_open` | Anti-replay is RFC-6479 over the per-sender counter. |
| **Group-key message** (kind 20 `HS_KEY`) | `uc_crypto/src/group.rs: GroupPlane::on_key_message` | a peer whose identity resolved (pairwise-sealed) | yes | two distinct message shapes share the kind, so the decoder must disambiguate hostile input | `uc_crypto_group_key` | The group key is symmetric — see the threat model §5. |
| **cnc control page** | `uc_protocol/src/v2/cnc.rs: read_cnc_header`, `read_cnc_app_id` | any local process that can read the instance directory | no — file permissions are the control | 8 KiB fixed layout since cnc `3.0` (2.8.0): page 1 as before, page 2 the `ServiceSlot[8]` band (`CNC_OFF_SERVICE_SLOTS = 4096`); magic + version gate; offsets pinned in two crates with assertion tests | `uc_protocol_cnc` | Only magic-checked, not integrity-protected: that is exactly why admin binding is taken from boot-time state instead (finding F4). |
| **Query payload split** (`service_id ++ query`, M14b) | `uc_protocol/src/v2/ipc.rs: split_query_payload` (reached from the node's query ring; `MSG_V2_BAD_SERVICE` answers an undeclared id) | a same-uid local client through the shmem query ring — **not** the gateway (`uc_gateway` never sets an id; the remote path reaches FSM 0 only) | no (boundary D) | one leading byte; an empty payload is `None`, never a panic; an id ≥ 8 or undeclared is refused by name | `ring_mpsc_record` (seeds `10-query-with-id`, `11-query-empty`) | Boundary D is not a security boundary; the check catches mistakes. |
| **cnc admin slot + auth line** | `uc_log/src/cnc.rs: CncPage::read_admin_req` (seqlock, `CNC_OFF_ADMIN_REQ`) and `read_admin_auth` (`CNC_OFF_ADMIN_AUTH = 3904`, 64 bytes) → `uc_node/src/node.rs: Consensus::verify_admin` | any local process that can write the instance directory | **yes with `[admin] auth = "hmac"`**: HMAC-SHA256 tag, `expiry_ns`, `seq > last_admin_seq`, and `instance_id`/`app_id` bound from boot-time state. `auth = "none"` = filesystem permissions only | fixed 64-byte line; acquire-load of the seqlock commit word so a torn write is never observed | `uc_crypto_admin` (property target over the signed-tag layout) | The tag covers `seq`, so no separate replay ring exists — the reasoning is in the spec's M12b "As built" amendment. |
| **`audit.jsonl`** | `uc_node/src/audit.rs: AuditLog::record` (append + `fsync`, before the answer is published); read offline by `uc2ctl audit` | written by the node; read by an operator | n/a (append-only file, not a parser of adversarial input) | one JSON object per line; a partial tail line is the crash signature, not an input | — | Anyone who can write the instance directory can truncate it. It is a record for an operator, not tamper-evident against a local attacker. |
| **Log frame header** | `uc_protocol/src/v2/frame.rs: read_header` (`HEADER_LEN = 32`, 32-byte alignment) | the archive/replay path and the apply reader; bytes originate from a peer's replication stream | inherits the datagram plane's answer | **deliberately caller-guarded** (`len >= HEADER_LEN`) — a hot-path reader; the fuzz target drives it *behind* the real caller's guard so it pins the contract rather than pretending it is absent | `uc_protocol_log_frame` | The atomic-after-write length prefix (0 = uncommitted) is the torn-record protection. Wire 0.7.0 **relaid** the header (`client_id: u32`, `seq: u32`, `time_ns: u64` in place of two `u64` ids): same length, same guard, no new field to bound. `time_ns` is peer-controlled data the state machine reads as "now" — a compromised leader can therefore lie about time to every replica, identically. That is the same trust a leader already has over command content and order, and is in the threat model's compromised-member bucket (out of model). |
| **`TIMER` frame body** (wire 0.7.0) | `uc_protocol/src/v2/frame.rs: read_timer_body` (`TIMER_BODY_LEN = 24`), reached from `uc_service/src/apply.rs`'s frame-type dispatch on a committed frame | a peer's replication stream, as above | as above | **total on any input**: returns `None` below 24 bytes, ignores a longer tail. Three opaque LE `u64`s (`identity_hash`, `timer_id`, `deadline_ns`) — no length, no index, no allocation derived from any of them; `identity_hash` is compared for equality against the local row's own hash and never used to index anything | `uc_protocol_timer_frame` | A forged `TIMER` frame (crypto off) can only deliver an `on_timer` call the receiving FSM was not expecting. `uc_service::Timed<S>` drops any instance not in its own log-derived pending set, so with the wrapper the forgery is a no-op; without it, an FSM that acts on unexpected timer ids is the exposure, and the answer is the same as for forged commands: turn on wire crypto. |
| **`svc_sched` ring record** (service → node, 2.11.0) | `uc_protocol/src/v2/ipc.rs: read_sched_record` (`SCHED_RECORD_LEN = 17`), drained by the node's consensus agent each pass from `svc_sched.<row>.ring` | the **local** service process for that row, through shmem | no — file permissions are the control, exactly as for every other IPC ring (boundary D) | fixed 17 bytes; total (`None` below the length); the leading `op` byte is validated against the four known values (`1` schedule, `2` cancel, `3` consumed, `4` table-consumed) and anything else decodes to `None` rather than a default | `uc_protocol_sched_record` | The first per-row ring the **node consumes** rather than produces, so it is a genuinely new local-write surface. A misbehaving local service can fill its own row's heap or cancel its own timers; both are bounded by that row and by `TIMERS_PER_PASS = 64` at the firing end, and neither reaches another row or another node. The `consumed` op is `pub(crate)` in `uc_service` so a state machine cannot forge one through the SDK, but the *ring bytes* are not a trust boundary: a process that can write the instance directory is host-compromise-equivalent (threat model §5). |
| **`ScheduleTable` payload** (`CLUSTER` kind 2, wire 0.7.0, 2.11.0) | `uc_protocol/src/v2/schedule.rs: decode_schedule_table`, reached through the `CLUSTER` prefix above and from the leader's `schedule apply` path over the staged file | a peer's replication stream (as a log frame), and a local writer of the instance directory (as the staged file) | inherits the datagram plane's answer on the wire; the staged file is digest-checked (row below) | **total on any input**: `None` on a short buffer, a `version` other than `1`, a `count` above `MAX_SCHEDULE_ENTRIES = 32`, a total length other than `8 + 33 × count`, an unknown `kind`, a zero `period_ns`, a `secs_of_day >= 86_400`, a non-zero `b` on an `at`/`once` entry, or a duplicate `(identity_hash, timer_id)`. The `Vec` it allocates is `with_capacity(count)` where `count <= 32` was already checked against the buffer's real length, so no allocation is driven by an unvalidated field. The recurrence arithmetic beside it (`next_after`, `latest_at_or_before`, `arm`) is saturating throughout — the overflows at the top of the `u64` range were **found by the fuzz target**, not reasoned about | `uc_protocol_schedule_table` | Byte layout frozen by `table_codec_pins_bytes_and_is_total`. A forged table (crypto off) can fire timers no operator asked for, at ids of the attacker's choosing, on any declared row — the same class as a forged `TIMER` frame, and the same answer: turn on wire crypto. `identity_hash` is only ever compared for equality against a declared row's own hash, never used as an index. An undecodable table is refused at `schedule apply` (`schedule_decode`, 42) and never appended, so the fail-stop on the archive path is not attacker-reachable ahead of the decoder. |
| **`schedules.pending`** (the staged schedule table, 2.11.0) | `uc_node/src/node.rs: Consensus::apply_schedule_table` — a `metadata` stat of `<instance_dir>/schedules.pending`, then a non-blocking (`O_NONBLOCK`) open, then a second `metadata` (`fstat`) on that handle, then a `take(MAX + 1)` read off the same handle, on the **consensus agent**; written by `uc2ctl schedule apply` (mode `0600`, fsync, rename) | any local process that can write the instance directory | **yes with `[admin] auth = "hmac"`, indirectly but completely**: the admin request's signed `(id, ip, port)` fields carry `uc_node::schedule_digest` — the first ten bytes of SHA-256 over the staged bytes — and the node recomputes it over the file it actually read, refusing `schedule_digest` (40) on any mismatch. So the 64-byte admin line authenticates a 1064-byte payload it cannot hold. Under `auth = "none"` it is trusted the way every admin request already is: filesystem permissions | size-checked against `MAX_SCHEDULE_TABLE_BYTES` (a full 32-entry table, `8 + 32 × 33` = 1064 B) and rejected unless it is a regular file, by name (`schedule_decode`, 42), before any read reaches it, at **three** layers: the initial `metadata` stat rejects a FIFO or an oversized file staged before the call even opened it; the open itself carries `O_NONBLOCK`, so a FIFO swapped in between the stat and the open returns immediately instead of blocking the thread that drives commit and elections; and a second `metadata` (`fstat`) on the opened handle re-checks `is_file()` and the size bound against what was actually opened, catching anything swapped in across that window — and the read is bounded (`take(MAX + 1)`) regardless, so nothing any of the three layers missed can turn into an unbounded read; then digest, then the total decoder above; then every entry's FSM hash must be a declared row (`schedule_unknown_fsm`, 43) or the **whole** table is refused | — (the bytes are covered by `uc_protocol_schedule_table`; this row is the file-handling path around it) | 80 bits of collision resistance is what three request fields have room for; it defends against an operator staging one file and signing another, not against a local attacker, who by the threat model's §5 already owns the host. Leader-only by construction: the file is node-local, so a follower neither forwards nor acts, and answers retry. The node deletes the file only after a successful append, which is what stops a re-presented request from appending the same table twice. |
| **`settings.pending`** (the staged settings record, 2.11.0) | `uc_node/src/node.rs: Consensus::apply_settings` — the same three-layer `read_staged` path the row above describes for `schedules.pending`, bounded by `MAX_SETTINGS_BYTES`, on the **consensus agent**; written by `uc2ctl settings apply` (mode `0600`, fsync, rename) | any local process that can write the instance directory | **yes with `[admin] auth = "hmac"`, indirectly but completely**: admin op 7's signed `(id, ip, port)` fields carry `uc_node::staged_digest` — the first ten bytes of SHA-256 over the staged bytes — recomputed over the file the node actually read, refusing `settings_digest` (44) on any mismatch | the same stat / `O_NONBLOCK` open / `fstat` / bounded-`take` ladder as `schedules.pending`, then the digest, then `decode_settings` (exact length, known version, known target byte, `settings_decode` 46), then `ClusterFsm::validate`'s static bounds (`settings_bounds` 47) | `uc_protocol_settings` | The record is 29 bytes, so the size ladder is not load-bearing here the way it is for a 1064-byte table — it is kept identical on purpose, because the two staged-file paths share `read_staged` and a divergence between them would be the bug. Every field's out-of-range value is **clamped at the point of use** on each host rather than refused, so a forged settings record degrades a cluster's throughput, never its agreement. |
| **Gateway TCP frames** | `uc_remote/src/frame.rs: decode_header` (24-byte header) + seven body decoders (`Hello`, `HelloOk`, `HelloRefused`, `ResponseMeta`, `Status`, `Leader`, `Retry`) via `uc_gateway/src/conn.rs` | **anyone who can open a TCP socket to the gateway port** | **no.** No client credential, no TLS in this release | `len > MAX_FRAME_LEN (1 MiB)` → `FrameError::TooLong` at decode, before allocation; `HELLO` must precede credits; per-connection credit window; `max_connections` (default 1024); a 1 s socket `WRITE_TIMEOUT`; `request_timeout` on outstanding completions | `uc_remote_frame` | The most exposed surface in the system. `FramedConn::read_frame`'s accumulate loop is *not* fuzzed (VERIFICATION §7) — the decoder is. |
| **`node.toml`** | `uc_node/src/config_file.rs: parse_str` | the operator (local file) | n/a | `serde(deny_unknown_fields)` on every section, then `preflight::check_semantics` — every named startup refusal | `uc_node_toml` | Not an adversary surface; it is the surface where an operator mistake becomes a named refusal instead of a broken cluster. |
| **`gateway.toml`** | `uc_gateway/src/config_file.rs: parse_str` (runs `EdgeConfig::validate` itself) | the operator | n/a | same | `uc_gateway_toml` | |
| **`/metrics`, `/healthz`, `/readyz`** | `uc_node/src/obs/http.rs: route` (fuzzed through the `cfg(any(test, fuzzing))` seam `route_raw`) | **anyone who can reach the metrics port** | **no — unauthenticated by design** | request buffer capped at `REQUEST_CAP = 4096` bytes; a connection deadline; only the request line is parsed; non-`GET` and unknown paths are 404; the status set is exactly `{200, 404, 503}` | `uc_node_http` | The bind address is the only control. See §2. |
| **Journal segments and records** | `uc_journal` segment header + record decoder, reached on every crash recovery | a local process with write access to the instance directory; otherwise, disk corruption | no | CRC per block, length checks, tail-preserving recovery | `uc_journal_record` | **CRC is integrity, not authenticity.** A local attacker who can write the directory is equivalent to host compromise (threat model §5). |
| **`StableValue` slots** (vote, term map, snapshot floor, output progress, config) | `uc_journal::stable_value` | as above | no | rotating two-slot write, CRC per slot | `uc_journal_stable_value` | Corruption here is a consensus-safety input, not merely data loss. |
| **Session envelope** | `uc_service/src/session.rs: Sessioned::{apply, install_snapshot}` | any remote client, through the gateway | no (the envelope is a dedup key, not a credential — `client_id` is client-chosen) | `SESSION_HEADER_LEN = 16` (`client_id ++ seq`); a shorter command is `TAG_EXPIRED`, never a panic; snapshot install bounded with `take(len)` after finding F3; replicated `SessionConfig` enforces client-count and byte budgets with LRU eviction | `uc_service_session` | Two real findings lived here (F2, F3). A hostile client can churn `client_id`s to force eviction — that is a liveness effect on *other* clients' dedup, not a correctness one. |
| **Typed state-machine tier (bincode)** | `uc_service/src/traits.rs`: the blanket `impl<S: StateMachine> RawStateMachine for S` — `bincode::serde::decode_from_slice::<S::Command>(cmd, bincode::config::standard())` | a remote client's opaque command bytes, **after** they are committed | no | **`bincode::config::standard()` is `Configuration<LittleEndian, Varint, NoLimit>` — no decode byte limit is configured.** What bounds it in practice: (a) the input slice is at most `max_payload` (≤ 1344 bytes, §3), (b) serde's `size_hint::cautious` caps a single container pre-allocation at 1 MiB, (c) a decode failure is `.expect("corrupt committed frame (fail-stop)")` | **not fuzzed here** — see the note | Fuzzing bincode would be fuzzing an external crate (VERIFICATION §7's "does not cover"). The honest consequence to state: with a typed state machine and untrusted remote clients, malformed bytes become *committed* bytes and every replica fail-stops identically on apply. **And queries do not even need a commit.** The same blanket impl decodes a query with `.expect("corrupt query frame (fail-stop)")` (`traits.rs`), reached **pre-commit** from `uc_service/src/apply.rs`'s query branch — which calls it while holding the state machine's `Mutex`. So one malformed `QUERY` frame from an unauthenticated TCP client, needing no quorum and no leadership, panics the apply thread and poisons that lock: a single-frame remote kill switch for a typed state machine. Documented, not fixed here — changing decode-error semantics is a design decision, parked as a follow-up. The raw tier is the workaround today. That is the documented "committed bytes are trusted" stance ([state-machine contract](/docs/reference/state-machine-contract.md)); the answer for genuinely untrusted input is the **raw tier**, which hands you the bytes and lets you reject them. |

## 2. Bind-address guidance

Three listening sockets, three different answers. `packaging/node.example.toml`
and `packaging/gateway.example.toml` carry the same advice inline, on the
`bind`, `listen` and `[metrics] bind` fields themselves — where an operator
copying an example actually reads it.

| Port | What it is | Expose to |
|---|---|---|
| **Node UDP** (`bind`, e.g. `10.0.0.10:9100`) | the replication and consensus plane | **the other nodes, and nothing else.** It must be the node's real address — identical to its own `members` entry, never a wildcard, or preflight refuses to start. With `[crypto].enabled = false` anyone who can reach this port can inject consensus traffic; with it on, an unlisted static key gets no session. Put it on a private subnet or a security group that admits only the member addresses. |
| **Gateway TCP** (`9200`+) | the remote client front door | **your application tier.** Unauthenticated and unencrypted in this release: treat reachability as authorization. Do not expose it to the internet; if clients are remote, terminate authentication and TLS in a proxy in front of it. |
| **Metrics HTTP** (`[metrics] bind`, e.g. `127.0.0.1:9600`) | `/metrics`, `/healthz`, `/readyz` | **loopback, or a private address your scraper can reach.** Unauthenticated by design — the endpoint discloses positions, terms, role and peer state. Absent `[metrics]` means no endpoint at all. |

## 3. The command payload ceiling

A command travels in **one datagram** — the node does not fragment frames. With
`MTU_DEFAULT = 1408` (the baseline rung; `2.12.0` pending: the effective
ceiling is **discovered per cluster** from the rung ladder `RUNGS = [1408,
8832, 8960]`, `MTU_BOUND = 8960`, not an operator dial — see [Wire protocol §
`PROBE` /
`PROBE_ACK`](../reference/wire-protocol.md#probe--probe_ack-bodies-wire-080-2120-pending))
and `DATAGRAM_HEADER_LEN = 16`, the frame header at `HEADER_LEN = 32` rounded
up to `FRAME_ALIGNMENT = 32`:

- **crypto off** — ≤ 1344 bytes at the baseline rung, up to ≤ 8896 bytes at
  the top rung once every path has proven it
- **crypto on** — ≤ 1312 bytes at the baseline rung, up to ≤ 8864 bytes at the
  top rung (`CRYPTO_OVERHEAD = 24`: an 8-byte counter plus a 16-byte GCM tag)

Through `2.11.0` this ceiling was pinned by a `node.toml` `max_payload` key,
checked at startup preflight (`uc_node::preflight::check_semantics`,
refusing with `PayloadExceedsMtu` naming the byte figures) — that key is
**retired since `2.12.0` pending, refused by name**, and preflight no longer
has an MTU check to make: the leader commits the cluster's discovered
ceiling into the replicated Settings record, and every node applies it at
commit (the sender's budget, the appender's door, and the live cnc
`payload_ceiling` word every submit reads).

## 4. What this page deliberately does not list

- **`bincode` internals** — an external crate reached through the seams
  above. That is a dependency posture question (`deny.toml`, the SBOM shipped
  with each release), not a target here.
- **Stateful sequences.** Every fuzz target is one decoder on one input.
  Sequences that drive the receiver's dispatch across terms, epochs and
  sessions belong to the simulation and the linearizability capstones
  ([VERIFICATION §2, §3](/docs/VERIFICATION.md)).
- **The IPC ring buffers' `unsafe` code** (`uc_protocol/src/ring/{spsc,mpsc,
  broadcast,common,futex}.rs`). Their **layout** is frozen by offset-pin tests.
  Since 2.7.0 the **MPSC claim-then-commit sequence is no longer unchecked**:
  `uc_protocol/tests/loom_mpsc.rs` is a loom model of that protocol (properties
  P1–P5, with mutation runs that prove the model can fail), its slot decoder
  has a fuzz target (`ring_mpsc_record`), and the preemption case — a producer
  stopped between claim and commit — is a unit test rather than a hope. What
  the loom model covers is the **protocol**, over a `Vec` of loom atomics: the
  mmap itself is still outside both loom and Miri (Miri cannot map a
  file-backed region), so mapping-level UB remains uncovered, and the model
  deliberately omits the tail-straddle padding path, the futex park/wake and
  the crc32. The **broadcast seqlock is still unmodelled** — deliberately, and
  the reason is written down: a faithful loom model of a seqlock-style
  overwrite race correctly fails under loom's full C++ semantics
  (`uc_log/tests/loom_frame.rs`'s header), so its absence is a design
  statement, not an oversight. SPSC's interleavings are likewise unmodelled.
  All of that is an open, stated residue
  ([VERIFICATION §11](/docs/VERIFICATION.md#11-what-is-not-verified)) and a
  named item in
  [the self-assessment's "focus here" list](self-assessment.md#4-what-an-external-review-should-focus-on).
